{% comment %}
  PLASTBAT — Macorner-style Shirt Product Page
  File: sections/plastbat-macorner-shirt-product.liquid
  Native Shopify options: Style / Color / Size.
  Includes size chart, personalization preview, shipping card, accordions,
  and manual product reviews with filters, 10-per-page pagination, and modal.
{% endcomment %}

{% liquid
  assign current_variant = product.selected_or_first_available_variant
  assign product_form_id = 'pbmac-form-' | append: section.id
  assign rating_object = product.metafields.reviews.rating.value
  assign rating_count = product.metafields.reviews.rating_count.value

  assign has_native_color_option = false
  assign has_native_size_option = false

  for pbmac_option in product.options_with_values
    assign pbmac_option_name = pbmac_option.name | downcase

    if pbmac_option_name contains 'color' or pbmac_option_name contains 'colour'
      assign has_native_color_option = true
    endif

    if pbmac_option_name contains 'size'
      assign has_native_size_option = true
    endif
  endfor

  assign pbmac_fallback_color_names = 'Black|Navy|Forest Green|Cardinal Red|Red|Orange|Carolina Blue|Light Blue|Sport Grey|Gold|White' | split: '|'
  assign pbmac_fallback_color_hexes = '#050505|#071D3A|#174F3C|#9E2638|#B8292F|#E45F21|#6486B8|#A6D5E5|#BFC4CC|#FFD000|#F2F2F2' | split: '|'
  assign pbmac_fallback_sizes = 'S|M|L|XL|2XL|3XL|4XL|5XL' | split: '|'

  assign pbmac_manual_review_count = 0
  assign pbmac_manual_rating_total = 0
  for pbmac_review_block in section.blocks
    if pbmac_review_block.type == 'manual_review'
      assign pbmac_manual_review_count = pbmac_manual_review_count | plus: 1
      assign pbmac_manual_rating_total = pbmac_manual_rating_total | plus: pbmac_review_block.settings.rating
    endif
  endfor

  if pbmac_manual_review_count > 0
    assign pbmac_manual_average = pbmac_manual_rating_total | times: 1.0 | divided_by: pbmac_manual_review_count
    assign pbmac_manual_average_rounded = pbmac_manual_average | round
  else
    assign pbmac_manual_average = 0
    assign pbmac_manual_average_rounded = 0
  endif

  if current_variant.compare_at_price > current_variant.price
    assign initial_save = current_variant.compare_at_price | minus: current_variant.price | times: 100 | divided_by: current_variant.compare_at_price
  else
    assign initial_save = 0
  endif
%}

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@500;600;700&display=swap" rel="stylesheet">

<section id="PBMac-{{ section.id }}" class="pbmac color-{{ section.settings.color_scheme }}">
  <div class="page-width pbmac__wrap">
    <div class="pbmac__grid">
      <div class="pbmac-gallery{% if section.settings.sticky_gallery %} pbmac-gallery--sticky{% endif %}">
        <div class="pbmac-gallery__main">
          {% if product.media.size > 0 %}
            {% for media in product.media %}
              <div class="pbmac-gallery__slide{% if forloop.first %} is-active{% endif %}" data-pbmac-media="{{ forloop.index0 }}" data-pbmac-media-id="{{ media.id }}" {% unless forloop.first %}hidden{% endunless %}>
                {% case media.media_type %}
                  {% when 'image' %}
                    {{ media.preview_image | image_url: width: 1600 | image_tag: loading: 'eager', widths: '480,720,960,1200,1600', sizes: '(min-width: 990px) 52vw, 100vw', class: 'pbmac-gallery__image', alt: media.alt | default: product.title }}
                  {% when 'video' %}
                    {{ media | video_tag: controls: true, image_size: '1200x', class: 'pbmac-gallery__video' }}
                  {% when 'external_video' %}
                    {{ media | external_video_tag: class: 'pbmac-gallery__video' }}
                  {% when 'model' %}
                    {{ media | model_viewer_tag: image_size: '1200x', reveal: 'interaction', toggleable: true, class: 'pbmac-gallery__video' }}
                  {% else %}
                    {{ media.preview_image | image_url: width: 1600 | image_tag: class: 'pbmac-gallery__image', alt: media.alt | default: product.title }}
                {% endcase %}
              </div>
            {% endfor %}
            {% if product.media.size > 1 %}
              <button type="button" class="pbmac-gallery__arrow pbmac-gallery__arrow--prev" data-pbmac-prev>‹</button>
              <button type="button" class="pbmac-gallery__arrow pbmac-gallery__arrow--next" data-pbmac-next>›</button>
            {% endif %}
          {% else %}
            {{ 'product-1' | placeholder_svg_tag: 'pbmac-gallery__placeholder' }}
          {% endif %}
        </div>

        {% if product.media.size > 1 %}
          <div class="pbmac-gallery__thumbs">
            {% for media in product.media %}
              <button type="button" class="pbmac-gallery__thumb{% if forloop.first %} is-active{% endif %}" data-pbmac-thumb="{{ forloop.index0 }}">
                {{ media.preview_image | image_url: width: 220 | image_tag: loading: 'lazy', class: 'pbmac-gallery__thumb-image', alt: media.alt | default: product.title }}
              </button>
            {% endfor %}
          </div>
        {% endif %}
      </div>

      <div class="pbmac-info">
        <h1 class="pbmac-title">{{ product.title }}</h1>

        {% if pbmac_manual_review_count > 0 %}
          <div class="pbmac-meta">
            <a href="#pbmac-reviews-{{ section.id }}" class="pbmac-rating pbmac-rating--manual" aria-label="Read product reviews">
              <span class="pbmac-rating__stars" aria-hidden="true">
                {% for i in (1..5) %}
                  <i class="{% if i <= pbmac_manual_average_rounded %}is-on{% endif %}">★</i>
                {% endfor %}
              </span>
              <em>({{ pbmac_manual_review_count }})</em>
            </a>
          </div>
        {% elsif rating_object != blank %}
          <div class="pbmac-meta">
            <a href="#pbmac-reviews-{{ section.id }}" class="pbmac-rating" aria-label="Read product reviews">
              <span>★★★★★</span>
              {% if rating_count != blank %}<em>({{ rating_count }})</em>{% endif %}
            </a>
          </div>
        {% endif %}

        <div class="pbmac-price-row" aria-live="polite">
          <span class="pbmac-compare{% unless current_variant.compare_at_price > current_variant.price %} is-hidden{% endunless %}" data-pbmac-compare>{% if current_variant.compare_at_price > current_variant.price %}{{ current_variant.compare_at_price | money_with_currency }}{% endif %}</span>
          <span class="pbmac-price" data-pbmac-price>{{ current_variant.price | money_with_currency }}</span>
          <span class="pbmac-save{% if initial_save == 0 %} is-hidden{% endif %}{% unless section.settings.show_save_badge %} pbmac-save--hidden{% endunless %}" data-pbmac-save>Save {{ initial_save }}%</span>
        </div>



        {% form 'product', product, id: product_form_id, class: 'pbmac-form' %}
          <input type="hidden" name="id" value="{{ current_variant.id }}" data-pbmac-variant-id>

          {% unless product.has_only_default_variant %}
            {% for option in product.options_with_values %}
              {% assign option_name_down = option.name | downcase %}
              {% assign option_type = 'pill' %}
              {% assign option_visible = true %}

              {% if option_name_down contains 'color' or option_name_down contains 'colour' %}
                {% assign option_type = 'color' %}
                {% assign option_visible = section.settings.show_color_option %}
              {% elsif option_name_down contains 'size' %}
                {% assign option_visible = section.settings.show_size_option %}
              {% elsif option_name_down contains 'style' %}
                {% assign option_visible = section.settings.show_style_option %}
              {% endif %}

              <fieldset
                class="pbmac-option"
                data-pbmac-option-index="{{ forloop.index0 }}"
                {% unless option_visible %}hidden{% endunless %}
              >
                <div class="pbmac-option__head">
                  <legend>
                    {{ option.name }}
                    {% if option_type == 'color' %}
                      <span class="pbmac-option__selected" data-pbmac-color-selected>{{ option.selected_value }}</span>
                    {% endif %}
                  </legend>
                  {% if option_name_down contains 'size' and section.settings.show_size_chart %}
                    <button type="button" class="pbmac-sizechart-link" data-pbmac-sizechart-open>Size Chart</button>
                  {% endif %}
                </div>

                {% if option_type == 'color' %}
                  <div class="pbmac-colors">
                    {% for option_value in option.values %}
                      {% assign value_name = option_value.name %}
                      <label
                        class="pbmac-color{% unless option_value.available %} is-unavailable{% endunless %}"
                        title="{{ value_name | escape }}"
                        aria-label="{{ value_name | escape }}"
                      >
                        <input
                          type="radio"
                          name="pbmac-opt-{{ section.id }}-{{ option.position }}"
                          value="{{ value_name | escape }}"
                          data-pbmac-option-value
                          data-pbmac-color-value
                          {% if option_value.selected %}checked{% endif %}
                        >
                        <span class="pbmac-color__outer">
                          <span
                            class="pbmac-color__swatch pbmac-color--{{ value_name | handleize }}"
                            {% if option_value.swatch.color != blank %}style="background:{{ option_value.swatch.color }}"{% endif %}
                          ></span>
                        </span>
                      </label>
                    {% endfor %}
                  </div>
                {% else %}
                  <div class="pbmac-pills">
                    {% for option_value in option.values %}
                      <label class="pbmac-pill{% unless option_value.available %} is-unavailable{% endunless %}"><input type="radio" name="pbmac-opt-{{ section.id }}-{{ option.position }}" value="{{ option_value.name | escape }}" data-pbmac-option-value {% if option_value.selected %}checked{% endif %}><span>{{ option_value.name }}</span></label>
                    {% endfor %}
                  </div>
                {% endif %}
              </fieldset>
            {% endfor %}
          {% endunless %}

          {% if section.settings.show_color_option %}
            {% unless has_native_color_option %}
            <fieldset class="pbmac-fallback-option pbmac-fallback-color">
              <div class="pbmac-fallback-option__head">
                <legend>
                  Color
                  <span data-pbmac-custom-color-selected>Black</span>
                </legend>
              </div>

              <div class="pbmac-fallback-colors" role="radiogroup" aria-label="Color">
                {% for pbmac_color_name in pbmac_fallback_color_names %}
                  {% assign pbmac_color_hex = pbmac_fallback_color_hexes[forloop.index0] %}
                  <label class="pbmac-fallback-color-pill" title="{{ pbmac_color_name | escape }}">
                    <input
                      type="radio"
                      name="properties[Color]"
                      value="{{ pbmac_color_name | escape }}"
                      data-pbmac-custom-color
                      {% if forloop.first %}checked{% endif %}
                    >
                    <span class="pbmac-fallback-color-pill__outer">
                      <i style="background: {{ pbmac_color_hex }};"></i>
                    </span>
                  </label>
                {% endfor %}
              </div>
            </fieldset>
            {% endunless %}
          {% endif %}

          {% if section.settings.show_size_option %}
            {% unless has_native_size_option %}
            <fieldset class="pbmac-fallback-option pbmac-fallback-size">
              <div class="pbmac-fallback-option__head">
                <legend>Size</legend>

                {% if section.settings.show_size_chart %}
                  <button type="button" class="pbmac-sizechart-link" data-pbmac-sizechart-open>
                    ⚖ Size Chart
                  </button>
                {% endif %}
              </div>

              <div class="pbmac-fallback-size-pills" role="radiogroup" aria-label="Size">
                {% for pbmac_size in pbmac_fallback_sizes %}
                  <label class="pbmac-fallback-size-pill">
                    <input
                      type="radio"
                      name="properties[Size]"
                      value="{{ pbmac_size | escape }}"
                      data-pbmac-custom-size
                      {% if forloop.first %}checked{% endif %}
                    >
                    <span>{{ pbmac_size }}</span>
                  </label>
                {% endfor %}
              </div>
            </fieldset>
            {% endunless %}
          {% endif %}

          {% if section.settings.show_personalization %}
            <div class="pbmac-personalization-card">
              <div class="pbmac-personalization-card__banner">
                <span class="pbmac-personalization-card__sparkles" aria-hidden="true">
                  <i class="pbmac-sparkle pbmac-sparkle--1">✦</i>
                  <i class="pbmac-sparkle pbmac-sparkle--2">✦</i>
                  <i class="pbmac-sparkle pbmac-sparkle--3">✦</i>
                </span>
                <strong>{{ section.settings.personalization_heading | escape }}</strong>
              </div>

              <div class="pbmac-personalization-card__body">
                <label for="PBMacPersonalization-{{ section.id }}">
                  {{ section.settings.personalization_field_label | escape }}
                  {% if section.settings.personalization_required %}<sup>*</sup>{% endif %}
                </label>

                <div class="pbmac-personalization-card__input-wrap">
                  <input
                    id="PBMacPersonalization-{{ section.id }}"
                    type="text"
                    class="pbmac-input"
                    name="properties[Personalization]"
                    maxlength="{{ section.settings.personalization_max }}"
                    placeholder="{{ section.settings.personalization_placeholder | escape }}"
                    data-pbmac-personalization
                    {% if section.settings.personalization_required %}required{% endif %}
                  >

                  <span class="pbmac-personalization-card__counter">
                    <b data-pbmac-personalization-count>0</b>/{{ section.settings.personalization_max }}
                  </span>
                </div>

                {% if section.settings.personalization_helper != blank %}
                  <p>{{ section.settings.personalization_helper | escape }}</p>
                {% endif %}
              </div>
            </div>
          {% endif %}

          {% if section.settings.show_personalization %}
            <div class="pbmac-customization-summary" data-pbmac-summary>
              <div class="pbmac-customization-summary__icon" aria-hidden="true">✓</div>
              <div class="pbmac-customization-summary__content">
                <strong>{{ section.settings.customization_summary_heading | escape }}</strong>
                <span><b data-pbmac-summary-color>—</b> · <em data-pbmac-summary-text>{{ section.settings.customization_summary_empty | escape }}</em></span>
              </div>
            </div>
          {% endif %}

          <div class="pbmac-buy-row pbmac-buy-row--compact">
            <div class="pbmac-qty">
              <button type="button" data-pbmac-minus aria-label="Decrease quantity">−</button>
              <input type="number" name="quantity" value="1" min="1" max="99" data-pbmac-qty aria-label="Quantity">
              <button type="button" data-pbmac-plus aria-label="Increase quantity">+</button>
            </div>

            <button type="submit" name="add" class="pbmac-atc pbmac-atc--inline" data-pbmac-atc {% unless current_variant.available %}disabled{% endunless %}>
              <span data-pbmac-atc-text>{% if current_variant.available %}{% if section.settings.show_personalization %}{{ section.settings.personalized_add_to_cart_text | escape }}{% else %}{{ section.settings.add_to_cart_text | escape }}{% endif %}{% else %}{{ section.settings.sold_out_text | escape }}{% endif %}</span>
              <span class="pbmac-atc__arrow" aria-hidden="true">→</span>
            </button>
          </div>
        {% endform %}

        <div class="pbmac-trust">
          <p><span class="pbmac-trust__check" aria-hidden="true">✓</span><span>{{ section.settings.trust_1 | escape }}</span></p>
          <p><span class="pbmac-trust__check" aria-hidden="true">✓</span><span>{{ section.settings.trust_2 | escape }}</span></p>
        </div>

        <div class="pbmac-accordions">
          <details class="pbmac-accordion" {% if section.settings.open_description %}open{% endif %}>
            <summary>
              <span class="pbmac-accordion__icon" aria-hidden="true">
                <svg viewBox="0 0 24 24">
                  <path d="M5 3.5h11l3 3V20.5H5z" fill="currentColor"/>
                  <path d="M8 9h8M8 12h8M8 15h6" fill="none" stroke="#fff" stroke-width="1.4" stroke-linecap="round"/>
                </svg>
              </span>
              {{ section.settings.description_heading | escape }}
              <span class="pbmac-accordion__chevron" aria-hidden="true">
                <svg viewBox="0 0 24 24">
                  <path d="M7 9.5 12 14.5 17 9.5" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
              </span>
            </summary>
            <div class="pbmac-accordion__content rte">
              {% if section.settings.description_body != blank %}
                {{ section.settings.description_body }}
              {% else %}
                {{ product.description }}
              {% endif %}
            </div>
          </details>

          <details class="pbmac-accordion">
            <summary>
              <span class="pbmac-accordion__icon" aria-hidden="true">
                <svg viewBox="0 0 24 24">
                  <path d="M3 6h11v9H3zM14 9h4l3 3v3h-7z" fill="currentColor"/>
                  <circle cx="7" cy="17" r="2" fill="currentColor"/>
                  <circle cx="18" cy="17" r="2" fill="currentColor"/>
                </svg>
              </span>
              {{ section.settings.shipping_heading | escape }}
              <span class="pbmac-accordion__chevron" aria-hidden="true">
                <svg viewBox="0 0 24 24">
                  <path d="M7 9.5 12 14.5 17 9.5" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
              </span>
            </summary>
            <div class="pbmac-accordion__content rte">{{ section.settings.shipping_text }}</div>
          </details>

          <details class="pbmac-accordion">
            <summary>
              <span class="pbmac-accordion__icon" aria-hidden="true">
                <svg viewBox="0 0 24 24">
                  <circle cx="12" cy="12" r="9" fill="currentColor"/>
                  <circle cx="12" cy="7.5" r="1.3" fill="#fff"/>
                  <path d="M12 11v6" stroke="#fff" stroke-width="1.8" stroke-linecap="round"/>
                </svg>
              </span>
              {{ section.settings.personalization_info_heading | escape }}
              <span class="pbmac-accordion__chevron" aria-hidden="true">
                <svg viewBox="0 0 24 24">
                  <path d="M7 9.5 12 14.5 17 9.5" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
              </span>
            </summary>
            <div class="pbmac-accordion__content rte">{{ section.settings.personalization_info }}</div>
          </details>
        </div>
      </div>
    </div>
  </div>

  {% if section.settings.show_size_chart %}
    <div class="pbmac-modal" data-pbmac-sizechart hidden><div class="pbmac-modal__backdrop" data-pbmac-sizechart-close></div><div class="pbmac-modal__dialog pbmac-sizechart"><button type="button" class="pbmac-modal__close" data-pbmac-sizechart-close>×</button><h3>Size Chart</h3><p>There might be ±1.5 inches difference due to manual measurement and production processes.</p><div class="pbmac-sizechart__table-wrap"><h4>Classic Tee</h4><table><thead><tr><th>Size</th><th>Chest (Front Only)</th><th>Body Length</th></tr></thead><tbody><tr><td>S</td><td>18</td><td>28</td></tr><tr><td>M</td><td>20</td><td>29</td></tr><tr><td>L</td><td>22</td><td>30</td></tr><tr><td>XL</td><td>24</td><td>31</td></tr><tr><td>2XL</td><td>26</td><td>32</td></tr><tr><td>3XL</td><td>28</td><td>33</td></tr><tr><td>4XL</td><td>30</td><td>34</td></tr><tr><td>5XL</td><td>32</td><td>35</td></tr></tbody></table></div></div></div>
  {% endif %}
</section>

{% if section.settings.show_reviews %}
<section id="pbmac-reviews-{{ section.id }}" class="pbmac-reviews" data-pbmac-reviews>
  {% liquid
    assign pbmac_review_count = 0
    assign pbmac_rating_total = 0
    assign pbmac_count_5 = 0
    assign pbmac_count_4 = 0
    assign pbmac_count_3 = 0
    assign pbmac_count_2 = 0
    assign pbmac_count_1 = 0
    assign pbmac_image_review_count = 0

    for block in section.blocks
      if block.type == 'manual_review'
        assign pbmac_review_count = pbmac_review_count | plus: 1
        assign pbmac_rating_total = pbmac_rating_total | plus: block.settings.rating

        if block.settings.image != blank
          assign pbmac_image_review_count = pbmac_image_review_count | plus: 1
        endif

        case block.settings.rating
          when 5
            assign pbmac_count_5 = pbmac_count_5 | plus: 1
          when 4
            assign pbmac_count_4 = pbmac_count_4 | plus: 1
          when 3
            assign pbmac_count_3 = pbmac_count_3 | plus: 1
          when 2
            assign pbmac_count_2 = pbmac_count_2 | plus: 1
          when 1
            assign pbmac_count_1 = pbmac_count_1 | plus: 1
        endcase
      endif
    endfor

    if pbmac_review_count > 0
      assign pbmac_average = pbmac_rating_total | times: 1.0 | divided_by: pbmac_review_count
    else
      assign pbmac_average = 0
    endif
  %}

  <div class="page-width">
    <div class="pbmac-review-tabs-top">
      <span class="is-active">Product Reviews</span>
    </div>

    <div class="pbmac-reviews-layout">
      <aside class="pbmac-reviews-card">
        <h2>Reviews for this item</h2>

        {% if pbmac_review_count > 0 %}
          <div class="pbmac-reviews-card__score">
            <strong>{{ pbmac_average | round: 1 }}</strong>
            <div>
              <span class="pbmac-reviews-card__stars">★★★★★</span>
              <small>{{ pbmac_review_count }} Review{% if pbmac_review_count != 1 %}s{% endif %}</small>
            </div>
          </div>

          {% assign pbmac_counts = pbmac_count_5 | append: ',' | append: pbmac_count_4 | append: ',' | append: pbmac_count_3 | append: ',' | append: pbmac_count_2 | append: ',' | append: pbmac_count_1 | split: ',' %}
          <div class="pbmac-histogram">
            {% for star in (1..5) reversed %}
              {% assign idx = 5 | minus: star %}
              {% assign count = pbmac_counts[idx] | plus: 0 %}
              {% assign pct = count | times: 100 | divided_by: pbmac_review_count %}
              {% assign display_pct = pct %}
              {% if count > 0 and pct == 0 %}
                {% assign display_pct = 1 %}
              {% endif %}
              <div class="pbmac-histogram__row">
                <span>{{ star }} Star</span>
                <div><i style="width:{{ display_pct }}%"></i></div>
                <b>{% if count > 0 and pct == 0 %}&lt;1%{% else %}{{ pct }}%{% endif %}</b>
              </div>
            {% endfor %}
          </div>

          <span class="pbmac-review-static-button">Write a Review</span>
        {% else %}
          <p class="pbmac-empty-review">No product reviews yet.</p>
        {% endif %}
      </aside>

      <div class="pbmac-reviews-content">
        {% if pbmac_image_review_count > 0 %}
          <div class="pbmac-review-images-head">
            <h3>Reviews with Images</h3>
            <div class="pbmac-review-images-arrows">
              <button type="button" data-pbmac-images-prev aria-label="Previous images">‹</button>
              <button type="button" data-pbmac-images-next aria-label="Next images">›</button>
            </div>
          </div>

          <div class="pbmac-review-images" data-pbmac-review-images>
            {% assign pbmac_review_index = 0 %}
            {% for block in section.blocks %}
              {% if block.type == 'manual_review' %}
                {% if block.settings.image != blank %}
                  <button
                    type="button"
                    class="pbmac-review-image-card"
                    data-pbmac-image-review-index="{{ pbmac_review_index }}"
                    aria-label="Open review from {{ block.settings.author | escape }}"
                  >
                    {{
                      block.settings.image
                      | image_url: width: 500
                      | image_tag:
                        loading: 'lazy',
                        widths: '180,260,360,500',
                        class: 'pbmac-review-image-card__image',
                        alt: block.settings.title
                    }}
                  </button>
                {% endif %}
                {% assign pbmac_review_index = pbmac_review_index | plus: 1 %}
              {% endif %}
            {% endfor %}
          </div>
        {% endif %}

        <div class="pbmac-topreviews-head">
          <h3>Top reviews</h3>
        </div>

        {% if pbmac_review_count > 0 %}
          <div class="pbmac-review-filters">
            <button class="is-active" type="button" data-rating="all">All Reviews ({{ pbmac_review_count }})</button>
            <button type="button" data-rating="5" {% if pbmac_count_5 == 0 %}disabled{% endif %}>5 Star ({{ pbmac_count_5 }})</button>
            <button type="button" data-rating="4" {% if pbmac_count_4 == 0 %}disabled{% endif %}>4 Star ({{ pbmac_count_4 }})</button>
            <button type="button" data-rating="3" {% if pbmac_count_3 == 0 %}disabled{% endif %}>3 Star ({{ pbmac_count_3 }})</button>
            <button type="button" data-rating="2" {% if pbmac_count_2 == 0 %}disabled{% endif %}>2 Star ({{ pbmac_count_2 }})</button>
            <button type="button" data-rating="1" {% if pbmac_count_1 == 0 %}disabled{% endif %}>1 Star ({{ pbmac_count_1 }})</button>
          </div>

          <div class="pbmac-review-list">
            {% assign pbmac_review_index = 0 %}
            {% for block in section.blocks %}
              {% if block.type == 'manual_review' %}
                <article
                  class="pbmac-review"
                  data-pbmac-review
                  data-rating="{{ block.settings.rating }}"
                  data-review-index="{{ pbmac_review_index }}"
                  role="button"
                  tabindex="0"
                  {{ block.shopify_attributes }}
                >
                  <div class="pbmac-review__headline">
                    <span class="pbmac-review__stars">
                      {% for i in (1..5) %}
                        <i class="{% if i <= block.settings.rating %}is-on{% endif %}">★</i>
                      {% endfor %}
                    </span>
                    <strong>{{ block.settings.author | escape }}</strong>
                  </div>

                  <div class="pbmac-review__meta">
                    {% if block.settings.verified %}<span>Verified Purchase</span>{% endif %}
                    {% if block.settings.date != blank %}<time>{{ block.settings.date | escape }}</time>{% endif %}
                  </div>

                  {% if block.settings.title != blank %}<h4>{{ block.settings.title | escape }}</h4>{% endif %}
                  {% if block.settings.body != blank %}<div class="pbmac-review__body rte">{{ block.settings.body }}</div>{% endif %}

                  {% if block.settings.image != blank %}
                    {{
                      block.settings.image
                      | image_url: width: 220
                      | image_tag:
                        loading: 'lazy',
                        widths: '90,140,220',
                        class: 'pbmac-review__thumb',
                        alt: block.settings.title
                    }}
                  {% endif %}

                  <template data-pbmac-review-modal-source>
                    <div class="pbmac-review-modal-source{% if block.settings.image == blank %} is-no-image{% endif %}">
                      {% if block.settings.image != blank %}
                        <div class="pbmac-review-modal-source__media">
                          {{
                            block.settings.image
                            | image_url: width: 1400
                            | image_tag:
                              loading: 'lazy',
                              widths: '600,900,1200,1400',
                              class: 'pbmac-review-modal-source__image',
                              alt: block.settings.title
                          }}
                        </div>
                      {% endif %}

                      <div class="pbmac-review-modal-source__details">
                        <div class="pbmac-review__headline">
                          <span class="pbmac-review__stars">
                            {% for i in (1..5) %}
                              <i class="{% if i <= block.settings.rating %}is-on{% endif %}">★</i>
                            {% endfor %}
                          </span>
                          <strong>{{ block.settings.author | escape }}</strong>
                        </div>

                        <div class="pbmac-review__meta">
                          {% if block.settings.verified %}<span>Verified Purchase</span>{% endif %}
                          {% if block.settings.date != blank %}<time>{{ block.settings.date | escape }}</time>{% endif %}
                        </div>

                        {% if block.settings.title != blank %}<h4>{{ block.settings.title | escape }}</h4>{% endif %}
                        {% if block.settings.body != blank %}<div class="rte">{{ block.settings.body }}</div>{% endif %}
                      </div>
                    </div>
                  </template>
                </article>

                {% assign pbmac_review_index = pbmac_review_index | plus: 1 %}
              {% endif %}
            {% endfor %}
          </div>

          <nav class="pbmac-pagination" aria-label="Review pages"></nav>
        {% elsif request.design_mode %}
          <div class="pbmac-empty-review">Add real customer reviews using “Manual review” blocks.</div>
        {% endif %}
      </div>
    </div>
  </div>

  <div class="pbmac-review-modal" hidden>
    <div class="pbmac-review-modal__backdrop" data-pbmac-review-close></div>
    <div class="pbmac-review-modal__dialog" role="dialog" aria-modal="true">
      <button type="button" class="pbmac-review-modal__close" data-pbmac-review-close>×</button>
      <button type="button" class="pbmac-review-modal__arrow pbmac-review-modal__arrow--prev" data-pbmac-review-prev>‹</button>
      <button type="button" class="pbmac-review-modal__arrow pbmac-review-modal__arrow--next" data-pbmac-review-next>›</button>
      <div class="pbmac-review-modal__content"></div>
    </div>
  </div>
</section>
{% endif %}



{% if section.settings.show_more_items %}
  {% liquid
    assign pbmac_more_source = blank

    if section.settings.more_items_collection != blank
      assign pbmac_more_source = collections[section.settings.more_items_collection]
    elsif product.collections.first != blank
      assign pbmac_more_source = product.collections.first
    endif
  %}

  {% if pbmac_more_source != blank %}
    <section class="pbmac-more-items" aria-labelledby="pbmac-more-items-title-{{ section.id }}">
      <div class="page-width">
        <div class="pbmac-more-items__inner">
          <h2 id="pbmac-more-items-title-{{ section.id }}">{{ section.settings.more_items_heading | escape }}</h2>

          <div class="pbmac-more-items__grid">
            {% assign pbmac_more_rendered = 0 %}
            {% for pbmac_more_product in pbmac_more_source.products %}
              {% if pbmac_more_product.id != product.id %}
                <a class="pbmac-more-card" href="{{ pbmac_more_product.url }}">
                  <div class="pbmac-more-card__media">
                    {% if pbmac_more_product.featured_image != blank %}
                      {{ pbmac_more_product.featured_image | image_url: width: 600 | image_tag: loading: 'lazy', class: 'pbmac-more-card__image', alt: pbmac_more_product.title }}
                    {% else %}
                      {{ 'product-1' | placeholder_svg_tag: 'pbmac-more-card__image pbmac-more-card__image--placeholder' }}
                    {% endif %}
                  </div>

                  <div class="pbmac-more-card__content">
                    <h3>{{ pbmac_more_product.title | escape }}</h3>

                    <div class="pbmac-more-card__stars" aria-hidden="true">★★★★★</div>

                    <div class="pbmac-more-card__price">
                      <span class="pbmac-more-card__sale">{{ pbmac_more_product.price | money }}</span>
                      {% if pbmac_more_product.compare_at_price > pbmac_more_product.price %}
                        <span class="pbmac-more-card__compare">{{ pbmac_more_product.compare_at_price | money }}</span>
                      {% endif %}
                    </div>
                  </div>
                </a>

                {% assign pbmac_more_rendered = pbmac_more_rendered | plus: 1 %}
                {% if pbmac_more_rendered >= section.settings.more_items_limit %}
                  {% break %}
                {% endif %}
              {% endif %}
            {% endfor %}
          </div>
        </div>
      </div>
    </section>
  {% endif %}
{% endif %}

{% if section.settings.show_newsletter %}
<section class="pbmac-newsletter" aria-labelledby="pbmac-newsletter-title-{{ section.id }}">
  <div class="page-width">
    <div class="pbmac-newsletter__inner">
      <div class="pbmac-newsletter__copy">
        <h2 id="pbmac-newsletter-title-{{ section.id }}">{{ section.settings.newsletter_heading | escape }}</h2>
        <p>{{ section.settings.newsletter_subtext | escape }}</p>
      </div>

      {% form 'customer', class: 'pbmac-newsletter__form' %}
        <input type="hidden" name="contact[tags]" value="newsletter">
        <div class="pbmac-newsletter__field">
          <input
            type="email"
            name="contact[email]"
            value="{% if customer %}{{ customer.email }}{% endif %}"
            placeholder="{{ section.settings.newsletter_placeholder | escape }}"
            autocomplete="email"
            autocorrect="off"
            autocapitalize="off"
            required
            aria-label="{{ section.settings.newsletter_placeholder | escape }}"
          >
          <button type="submit">{{ section.settings.newsletter_button | escape }}</button>
        </div>

        {% if form.posted_successfully? %}
          <p class="pbmac-newsletter__success">{{ section.settings.newsletter_success | escape }}</p>
        {% endif %}

        {% if form.errors %}
          <div class="pbmac-newsletter__error">{{ form.errors | default_errors }}</div>
        {% endif %}
      {% endform %}
    </div>
  </div>
</section>
{% endif %}

<style>
  #PBMac-{{ section.id }}{--dark:#2f2b31;--muted:#7f7881;--orange:#f16422;background:rgb(var(--color-background,250,247,242));color:var(--dark);font-family:var(--font-body-family,inherit)}
  #PBMac-{{ section.id }} *{box-sizing:border-box}.is-hidden{display:none!important}.pbmac__wrap{padding-top:{{ section.settings.padding_top }}px;padding-bottom:{{ section.settings.padding_bottom }}px}.pbmac__grid{display:grid;grid-template-columns:minmax(0,1.08fr) minmax(420px,.92fr);gap:clamp(34px,4vw,60px);align-items:start}
  .pbmac-gallery{min-width:0;align-self:start}.pbmac-gallery--sticky{position:sticky;top:{{ section.settings.sticky_top }}px}.pbmac-gallery__main{position:relative;aspect-ratio:1/1;overflow:hidden;border:1px solid #ded8d3;border-radius:10px;background:#f6f6f6}.pbmac-gallery__slide{width:100%;height:100%}.pbmac-gallery__image,.pbmac-gallery__video,.pbmac-gallery__placeholder{width:100%;height:100%;display:block;object-fit:contain}.pbmac-gallery__arrow{position:absolute;top:50%;width:48px;height:48px;transform:translateY(-50%);border:1px solid #ded7d1;border-radius:999px;background:rgba(255,255,255,.96);color:#342f34;display:grid;place-items:center;font-size:32px;cursor:pointer;box-shadow:0 5px 18px rgba(0,0,0,.08)}.pbmac-gallery__arrow--prev{left:16px}.pbmac-gallery__arrow--next{right:16px}.pbmac-gallery__thumbs{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:10px;margin-top:10px}.pbmac-gallery__thumb{padding:0;aspect-ratio:1/1;overflow:hidden;border:1px solid #e0dad5;border-radius:8px;background:#fff;opacity:.7;cursor:pointer}.pbmac-gallery__thumb.is-active{opacity:1;border:2px solid #3f3532}.pbmac-gallery__thumb-image{width:100%;height:100%;object-fit:cover;display:block}
  .pbmac-title{margin:0;color:#262329;font-family:var(--font-heading-family,inherit);font-size:clamp(30px,2.6vw,42px);line-height:1.08;font-weight:760;letter-spacing:-.025em}.pbmac-meta{margin-top:11px}.pbmac-rating{display:inline-flex;align-items:center;gap:6px;color:#4e474b;text-decoration:none;font-size:13px}.pbmac-rating span{color:var(--orange);letter-spacing:1px}.pbmac-rating em{font-style:normal;color:#90878b}.pbmac-price-row{display:flex;align-items:center;flex-wrap:wrap;gap:9px;margin-top:15px}.pbmac-price{font-size:29px;font-weight:780}.pbmac-compare{color:#aaa1a3;text-decoration:line-through;font-size:17px}.pbmac-save{color:#e7632c;font-weight:750;font-size:13px}
  .pbmac-bundle{margin-top:18px;padding:14px 15px;border:1px solid #e3ddd8;border-radius:10px;background:#fff}.pbmac-bundle>strong{display:block;font-size:14px;margin-bottom:9px}.pbmac-bundle>div{display:flex;flex-wrap:wrap;gap:8px}.pbmac-bundle span{padding:7px 9px;border-radius:6px;background:#fff5ef;color:#b55329;font-size:12px;font-weight:700}.pbmac-bundle small{display:block;margin-top:8px;color:#948b8f;font-size:11.5px}
  .pbmac-shipping{margin-top:16px;border:1px solid #e3ddd8;border-radius:10px;background:#fff;overflow:hidden}.pbmac-shipping__head{padding:13px 14px;display:flex;gap:10px;align-items:flex-start;border-bottom:1px solid #eee8e3}.pbmac-shipping__head strong{display:block;font-size:13px}.pbmac-shipping__head small{display:block;margin-top:2px;color:#8c8386;font-size:11px}.pbmac-shipping__timeline{display:grid;grid-template-columns:repeat(3,1fr);padding:13px 10px}.pbmac-shipping__timeline>div{display:flex;align-items:flex-start;gap:7px;padding:0 8px;border-right:1px solid #eee8e3}.pbmac-shipping__timeline>div:last-child{border-right:0}.pbmac-shipping__timeline i{width:22px;height:22px;flex:0 0 22px;display:grid;place-items:center;border-radius:999px;background:#f4f0ed;color:#5e5557;font-style:normal;font-size:10px;font-weight:800}.pbmac-shipping__timeline b{display:block;font-size:10.5px}.pbmac-shipping__timeline small{display:block;margin-top:2px;color:#9b9295;font-size:9.5px}
  .pbmac-form{margin-top:20px}.pbmac-option{margin:0 0 19px;padding:0;border:0}.pbmac-option__head{display:flex;align-items:center;justify-content:space-between;margin-bottom:9px}.pbmac-option__head legend{font-size:14px;font-weight:760}.pbmac-sizechart-link{padding:0;border:0;background:transparent;text-decoration:underline;color:#5d555a;font:inherit;font-size:12px;cursor:pointer}.pbmac-pills{display:flex;flex-wrap:wrap;gap:8px}.pbmac-pill{position:relative}.pbmac-pill input{position:absolute;opacity:0;pointer-events:none}.pbmac-pill span{min-width:56px;min-height:42px;padding:0 13px;display:inline-flex;align-items:center;justify-content:center;border:1px solid #d9d2cd;border-radius:7px;background:#fff;color:#40393c;font-size:12.5px;font-weight:650;cursor:pointer}.pbmac-pill input:checked+span{border-color:#332e32;box-shadow:inset 0 0 0 1px #332e32}.pbmac-pill.is-unavailable{opacity:.35}
  .pbmac-colors{display:flex;flex-wrap:wrap;gap:9px}.pbmac-color{position:relative;width:64px;cursor:pointer;text-align:center}.pbmac-color input{position:absolute;opacity:0;pointer-events:none}.pbmac-color__swatch{width:43px;height:43px;display:block;margin:0 auto 5px;border-radius:6px;border:1px solid rgba(0,0,0,.15);background:#ddd}.pbmac-color input:checked+.pbmac-color__swatch{box-shadow:0 0 0 2px #fff,0 0 0 4px #332e32}.pbmac-color__name{color:#5f575b;font-size:10px;line-height:1.1}.pbmac-color.is-unavailable{opacity:.35}.pbmac-color--black{background:#111}.pbmac-color--navy{background:#17233f}.pbmac-color--forest-green{background:#234c37}.pbmac-color--cardinal-red{background:#9f2730}.pbmac-color--red{background:#d72d2d}.pbmac-color--orange{background:#e8732d}.pbmac-color--carolina-blue{background:#7faad7}.pbmac-color--light-blue{background:#a8c9e9}.pbmac-color--sport-grey,.pbmac-color--sport-gray{background:#b8b7b6}.pbmac-color--gold{background:#dfac2f}.pbmac-color--white{background:#fff}.pbmac-color--light-pink{background:#edb6c6}
  .pbmac-personalization{margin:2px 0 17px;padding:13px;border:1px solid #e5ded9;border-radius:9px;background:#faf8f6}.pbmac-personalization__head{display:flex;align-items:center;gap:7px;margin-bottom:8px}.pbmac-personalization__head strong{font-size:13px}.pbmac-input{width:100%;height:46px;padding:0 13px;border:1px solid #d9d2cd;border-radius:7px;background:#fff;color:#3e383b;font:inherit;font-size:13px;outline:0}.pbmac-buy-row{display:grid;grid-template-columns:116px minmax(0,1fr);gap:10px;margin-top:16px}.pbmac-qty{height:50px;display:grid;grid-template-columns:36px 1fr 36px;border:1px solid #d9d2cd;border-radius:999px;background:#fff;overflow:hidden}.pbmac-qty button{border:0;background:transparent;font-size:20px;cursor:pointer}.pbmac-qty input{width:100%;border:0;background:transparent;text-align:center;font-weight:700;outline:0;-moz-appearance:textfield}.pbmac-qty input::-webkit-inner-spin-button,.pbmac-qty input::-webkit-outer-spin-button{-webkit-appearance:none;margin:0}.pbmac-preview{height:50px;border:1px solid #373136;border-radius:999px;background:#fff;color:#393238;font:inherit;font-size:13px;font-weight:750;cursor:pointer}.pbmac-atc{width:100%;height:54px;margin-top:10px;border:0;border-radius:999px;background:var(--orange);color:#fff;font:inherit;font-size:14px;font-weight:800;cursor:pointer}.pbmac-atc:disabled{opacity:.45}.pbmac-trust{margin:15px 0 2px;padding:0 2px}.pbmac-trust p{margin:7px 0;color:#61595d;font-size:11.5px}.pbmac-accordions{margin-top:15px;border-top:1px solid #e6dfda}.pbmac-accordion{border-bottom:1px solid #e6dfda}.pbmac-accordion summary{min-height:52px;display:grid;grid-template-columns:24px minmax(0,1fr) auto;gap:6px;align-items:center;list-style:none;cursor:pointer;color:#3f393c;font-size:13px;font-weight:700}.pbmac-accordion summary::-webkit-details-marker{display:none}.pbmac-accordion__content{padding:0 4px 17px;color:#71696d;font-size:12.5px;line-height:1.62}
  .pbmac-modal[hidden],.pbmac-review-modal[hidden]{display:none!important}.pbmac-modal,.pbmac-review-modal{position:fixed;inset:0;z-index:9999;display:grid;place-items:center;padding:20px}.pbmac-modal__backdrop,.pbmac-review-modal__backdrop{position:absolute;inset:0;background:rgba(45,39,43,.7)}.pbmac-modal__dialog{position:relative;z-index:1;width:min(720px,calc(100vw - 40px));max-height:calc(100vh - 50px);overflow:auto;padding:26px;border-radius:14px;background:#fff;box-shadow:0 22px 70px rgba(0,0,0,.24)}.pbmac-modal__close{position:absolute;right:12px;top:10px;width:34px;height:34px;border:0;border-radius:999px;background:#f5f2f0;font-size:22px;cursor:pointer}.pbmac-sizechart__table-wrap{margin-top:18px;overflow-x:auto}.pbmac-sizechart table{width:100%;border-collapse:collapse;font-size:12px}.pbmac-sizechart th,.pbmac-sizechart td{padding:9px 10px;border:1px solid #e4ddd8;text-align:center}.pbmac-sizechart th{background:#f5f2f0}.pbmac-preview-card{margin-top:20px;padding:20px;border:1px solid #e3ddd8;border-radius:12px;background:#faf8f6;display:grid;gap:8px}.pbmac-preview-card em{margin-top:7px;color:#a15332;font-style:normal;font-weight:700}
  .pbmac-reviews{padding:34px 0 46px;background:#faf8f6;color:#514a4e;font-family:var(--font-body-family,inherit)}.pbmac-reviews__panel{width:min(100%,1080px);margin:0 auto;padding:24px 26px;border:1px solid #e5d8cc;border-radius:16px;background:#fff;display:grid;grid-template-columns:250px minmax(0,1fr);gap:34px}.pbmac-reviews__summary{padding-right:24px;border-right:1px solid #eee6e0}.pbmac-reviews__summary h2{margin:0 0 16px;font-size:22px}.pbmac-reviews__score{display:flex;gap:13px;align-items:center;margin-bottom:20px}.pbmac-reviews__score>strong{font-size:36px;line-height:1}.pbmac-reviews__score>div{display:grid;gap:4px}.pbmac-reviews__score span{color:var(--orange);letter-spacing:1px}.pbmac-reviews__score small{color:#9a9194;font-size:11.5px}.pbmac-histogram{display:grid;gap:8px}.pbmac-histogram__row{display:grid;grid-template-columns:42px 1fr 34px;gap:8px;align-items:center;font-size:11px;color:#837a7d}.pbmac-histogram__row>div{height:6px;background:#eee7e2;border-radius:99px;overflow:hidden}.pbmac-histogram__row i{display:block;height:100%;background:#c58a52}.pbmac-histogram__row b{text-align:right;font-weight:600}.pbmac-reviews__main h3{margin:0 0 12px;font-size:20px}.pbmac-review-filters{display:flex;flex-wrap:wrap;gap:7px;margin-bottom:6px}.pbmac-review-filters button{min-height:36px;padding:0 13px;border:1px solid #e8dfd8;border-radius:999px;background:#fff;color:#796f74;font:inherit;font-size:11px;cursor:pointer}.pbmac-review-filters button.is-active{border-color:#4c4448;box-shadow:inset 0 0 0 1px #4c4448}.pbmac-review-filters button:disabled{opacity:.3}.pbmac-review{padding:17px 0;border-bottom:1px solid #eee6e0;cursor:pointer}.pbmac-review[hidden]{display:none!important}.pbmac-review__headline{display:flex;gap:8px;align-items:center}.pbmac-review__stars{display:flex;font-size:13px;color:#ddd3cd}.pbmac-review__stars i{font-style:normal}.pbmac-review__stars .is-on{color:var(--orange)}.pbmac-review__headline strong{font-size:12.5px}.pbmac-review__meta{display:flex;gap:7px;margin-top:6px;color:#a09598;font-size:10.5px}.pbmac-review h4{margin:9px 0 4px;font-size:13px}.pbmac-review__body{color:#665d61;font-size:12.5px;line-height:1.55}.pbmac-review__thumb{width:70px;height:70px;margin-top:9px;object-fit:cover;border-radius:8px;border:1px solid #e3d9d1}.pbmac-pagination{display:flex;gap:5px;margin-top:14px}.pbmac-pagination button{min-width:31px;height:31px;padding:0 8px;border:1px solid #e5ddd7;border-radius:999px;background:#fff;color:#796f74;font:inherit;font-size:11px;cursor:pointer}.pbmac-pagination button.is-active{background:#52484d;border-color:#52484d;color:#fff}.pbmac-review-modal{position:fixed;inset:0;z-index:10000;display:grid;place-items:center;padding:20px}.pbmac-review-modal__dialog{position:relative;z-index:1;width:min(860px,calc(100vw - 54px));max-height:min(620px,calc(100vh - 56px));border-radius:14px;background:#fff;box-shadow:0 22px 70px rgba(0,0,0,.28)}.pbmac-review-modal__content{max-height:min(620px,calc(100vh - 56px));overflow:hidden;border-radius:inherit}.pbmac-review-modal__close{position:absolute;top:-14px;right:-14px;z-index:4;width:36px;height:36px;border:1px solid #e6ded8;border-radius:999px;background:#fff;font-size:23px;cursor:pointer}.pbmac-review-modal__arrow{position:absolute;top:50%;z-index:4;width:40px;height:40px;transform:translateY(-50%);border:1px solid #ece3dd;border-radius:999px;background:#fff;font-size:28px;cursor:pointer}.pbmac-review-modal__arrow--prev{left:-20px}.pbmac-review-modal__arrow--next{right:-20px}.pbmac-review-modal-source{display:grid;grid-template-columns:49% 51%;min-height:390px;max-height:min(620px,calc(100vh - 56px))}.pbmac-review-modal-source.is-no-image{grid-template-columns:1fr}.pbmac-review-modal-source__media{padding:16px;border-right:1px solid #eee6df;background:#fffdfb;display:grid;place-items:center;overflow:hidden}.pbmac-review-modal-source__image{width:100%;height:100%;max-height:500px;object-fit:contain;border-radius:8px}.pbmac-review-modal-source__details{padding:22px 22px 26px;overflow-y:auto;color:#554944}body.pbmac-modal-open{overflow:hidden!important}
  @media(max-width:989px){#PBMac-{{ section.id }} .pbmac__grid{grid-template-columns:1fr;gap:26px}.pbmac-gallery--sticky{position:static}.pbmac-reviews__panel{grid-template-columns:220px minmax(0,1fr);gap:24px}}
  @media(max-width:749px){#PBMac-{{ section.id }} .pbmac__wrap{padding-top:16px}.pbmac-gallery__thumbs{display:flex;overflow-x:auto}.pbmac-gallery__thumb{width:64px;flex:0 0 64px}.pbmac-title{font-size:30px}.pbmac-shipping__timeline{grid-template-columns:1fr}.pbmac-shipping__timeline>div{border-right:0;border-bottom:1px solid #eee8e3;padding:8px}.pbmac-buy-row{grid-template-columns:104px minmax(0,1fr)}.pbmac-reviews__panel{grid-template-columns:1fr;gap:22px;padding:18px 14px}.pbmac-reviews__summary{padding-right:0;padding-bottom:20px;border-right:0;border-bottom:1px solid #eee6e0}.pbmac-review-modal__dialog{width:min(620px,calc(100vw - 28px));max-height:calc(100vh - 28px)}.pbmac-review-modal__content{max-height:calc(100vh - 28px);overflow-y:auto}.pbmac-review-modal-source{display:block;min-height:0;max-height:none}.pbmac-review-modal-source__media{height:280px;border-right:0;border-bottom:1px solid #eee6df}.pbmac-review-modal-source__details{padding:18px}.pbmac-review-modal__arrow--prev{left:8px}.pbmac-review-modal__arrow--next{right:8px}.pbmac-review-modal__close{top:8px;right:8px}}

  /* V2 — MACORNER-LIKE COLOR SWATCHES */
  .pbmac-option__selected{
    margin-left:5px;
    color:#a39a9d;
    font-size:12px;
    font-weight:500;
  }
  .pbmac-colors{
    display:flex!important;
    flex-wrap:wrap!important;
    gap:8px!important;
    align-items:center;
  }
  .pbmac-color{
    width:auto!important;
    cursor:pointer;
  }
  .pbmac-color__outer{
    width:56px;
    height:36px;
    padding:3px;
    display:block;
    border:1px solid #ded7d2;
    border-radius:999px;
    background:#fff;
    transition:border-color .15s ease,box-shadow .15s ease;
  }
  .pbmac-color__swatch{
    width:100%!important;
    height:100%!important;
    margin:0!important;
    display:block;
    border:0!important;
    border-radius:999px!important;
  }
  .pbmac-color input:checked + .pbmac-color__outer{
    border-color:#3e373b;
    box-shadow:0 0 0 1px #3e373b;
  }
  .pbmac-color:hover .pbmac-color__outer{
    border-color:#857b7e;
  }
  .pbmac-color__name{
    display:none!important;
  }

  /* V2 — REVIEWS LAYOUT */
  .pbmac-reviews{
    padding:28px 0 44px!important;
    background:#faf8f6!important;
  }

  .pbmac-review-tabs-top{
    width:min(100%,1180px);
    margin:0 auto 16px;
    border-bottom:1px solid #e8e0da;
  }
  .pbmac-review-tabs-top span{
    position:relative;
    display:inline-flex;
    padding:0 0 12px;
    color:#4a4246;
    font-size:13px;
    font-weight:700;
  }
  .pbmac-review-tabs-top span::after{
    content:'';
    position:absolute;
    left:0;right:0;bottom:-1px;
    height:2px;
    background:#4a4246;
  }

  .pbmac-reviews-layout{
    width:min(100%,1180px);
    margin:0 auto;
    display:grid;
    grid-template-columns:320px minmax(0,1fr);
    gap:42px;
    align-items:start;
  }

  .pbmac-reviews-card{
    padding:24px 22px;
    border-radius:8px;
    background:#f3f0ec;
  }
  .pbmac-reviews-card h2{
    margin:0 0 22px;
    color:#332f32;
    font-size:16px;
    line-height:1.2;
  }
  .pbmac-reviews-card__score{
    display:flex;
    align-items:center;
    gap:18px;
    margin-bottom:22px;
  }
  .pbmac-reviews-card__score > strong{
    color:#3b2d33;
    font-family:Georgia,serif;
    font-size:48px;
    line-height:1;
    font-weight:500;
  }
  .pbmac-reviews-card__score > div{
    display:grid;
    gap:5px;
  }
  .pbmac-reviews-card__stars{
    color:#f16422;
    letter-spacing:2px;
    font-size:18px;
  }
  .pbmac-reviews-card__score small{
    color:#443d40;
    font-size:12px;
  }

  .pbmac-reviews-card .pbmac-histogram{
    display:grid;
    gap:13px;
  }
  .pbmac-reviews-card .pbmac-histogram__row{
    display:grid;
    grid-template-columns:42px minmax(0,1fr) 38px;
    gap:10px;
    align-items:center;
    color:#50494c;
    font-size:11px;
  }
  .pbmac-reviews-card .pbmac-histogram__row > div{
    height:7px;
    border-radius:99px;
    background:#e3ddd7;
  }
  .pbmac-reviews-card .pbmac-histogram__row i{
    background:#ff7040;
  }
  .pbmac-review-static-button{
    min-height:44px;
    margin-top:20px;
    padding:0 15px;
    border:1px solid #393337;
    border-radius:999px;
    display:inline-flex;
    align-items:center;
    justify-content:center;
    color:#393337;
    font-size:12px;
    font-weight:700;
  }

  .pbmac-review-images-head,
  .pbmac-topreviews-head{
    display:flex;
    align-items:center;
    justify-content:space-between;
  }
  .pbmac-review-images-head h3,
  .pbmac-topreviews-head h3{
    margin:0;
    color:#2f2b2e;
    font-size:20px;
  }
  .pbmac-review-images-arrows{
    display:flex;
    gap:10px;
  }
  .pbmac-review-images-arrows button{
    width:34px;
    height:34px;
    border:0;
    background:transparent;
    color:#3f383c;
    font-size:28px;
    cursor:pointer;
  }
  .pbmac-review-images{
    display:flex;
    gap:10px;
    margin-top:14px;
    overflow-x:auto;
    scroll-behavior:smooth;
    scrollbar-width:none;
  }
  .pbmac-review-images::-webkit-scrollbar{display:none}
  .pbmac-review-image-card{
    width:168px;
    flex:0 0 168px;
    aspect-ratio:1/1;
    padding:0;
    overflow:hidden;
    border:0;
    border-radius:8px;
    background:#f4f1ef;
    cursor:pointer;
  }
  .pbmac-review-image-card__image{
    width:100%;
    height:100%;
    display:block;
    object-fit:cover;
  }

  .pbmac-topreviews-head{
    margin-top:28px;
    margin-bottom:12px;
  }

  @media(max-width:989px){
    .pbmac-reviews-layout{
      grid-template-columns:280px minmax(0,1fr);
      gap:28px;
    }
    .pbmac-review-image-card{
      width:150px;
      flex-basis:150px;
    }
  }

  @media(max-width:749px){
    .pbmac-review-tabs-top,
    .pbmac-reviews-layout{
      width:100%;
    }
    .pbmac-reviews-layout{
      grid-template-columns:1fr;
      gap:22px;
    }
    .pbmac-reviews-card{
      padding:20px 18px;
    }
    .pbmac-review-image-card{
      width:138px;
      flex-basis:138px;
    }
  }


  /* V3 CLEAN TOP AREA */
  #PBMac-{{ section.id }},
  #PBMac-{{ section.id }} button,
  #PBMac-{{ section.id }} input,
  #PBMac-{{ section.id }} select,
  #PBMac-{{ section.id }} textarea {
    font-family: "Montserrat", "Avenir Next", "Helvetica Neue", Arial, sans-serif !important;
  }

  #PBMac-{{ section.id }} .pbmac-title {
    max-width: 720px;
    color: #2f2b2d;
    font-family: "Montserrat", "Avenir Next", "Helvetica Neue", Arial, sans-serif !important;
    font-size: clamp(30px, 2.35vw, 39px);
    line-height: 1.16;
    letter-spacing: -.035em;
    font-weight: 700;
  }

  #PBMac-{{ section.id }} .pbmac-meta {
    margin-top: 9px;
  }

  #PBMac-{{ section.id }} .pbmac-price-row {
    margin-top: 14px;
    margin-bottom: 24px;
  }

  #PBMac-{{ section.id }} .pbmac-price {
    color: #302c2e;
    font-size: 30px;
    font-weight: 700;
    letter-spacing: -.025em;
  }

  #PBMac-{{ section.id }} .pbmac-compare {
    font-size: 16px;
  }

  #PBMac-{{ section.id }} .pbmac-save {
    font-size: 12px;
    font-weight: 700;
  }

  #PBMac-{{ section.id }} .pbmac-form {
    margin-top: 0;
  }

  #PBMac-{{ section.id }} .pbmac-option {
    margin-bottom: 21px;
  }

  #PBMac-{{ section.id }} .pbmac-option__head {
    margin-bottom: 10px;
  }

  #PBMac-{{ section.id }} .pbmac-option__head legend {
    color: #302c2e;
    font-size: 14px;
    font-weight: 650;
  }

  #PBMac-{{ section.id }} .pbmac-option__selected {
    margin-left: 5px;
    color: #9d9598;
    font-size: 12px;
    font-weight: 500;
  }

  /* Color pills like the reference */
  #PBMac-{{ section.id }} .pbmac-colors {
    gap: 8px !important;
  }

  #PBMac-{{ section.id }} .pbmac-color__outer {
    width: 56px !important;
    height: 36px !important;
    padding: 3px !important;
    border: 1px solid #e1dbd6 !important;
    border-radius: 999px !important;
    background: #fff !important;
  }

  #PBMac-{{ section.id }} .pbmac-color__swatch {
    width: 100% !important;
    height: 100% !important;
    margin: 0 !important;
    border: 0 !important;
    border-radius: 999px !important;
  }

  #PBMac-{{ section.id }} .pbmac-color input:checked + .pbmac-color__outer {
    border-color: #393337 !important;
    box-shadow: 0 0 0 1px #393337 !important;
  }

  #PBMac-{{ section.id }} .pbmac-color__name {
    display: none !important;
  }

  #PBMac-{{ section.id }} .pbmac-pill span {
    min-height: 42px;
    padding: 0 15px;
    border-color: #ddd7d2;
    border-radius: 8px;
    color: #443e41;
    font-size: 12px;
    font-weight: 600;
  }

  #PBMac-{{ section.id }} .pbmac-personalization {
    padding: 13px 0 0;
    border: 0;
    border-top: 1px solid #ece6e1;
    border-radius: 0;
    background: transparent;
  }

  #PBMac-{{ section.id }} .pbmac-personalization__head strong {
    font-size: 13px;
    font-weight: 650;
  }

  #PBMac-{{ section.id }} .pbmac-input {
    height: 48px;
    border-radius: 8px;
    font-size: 13px;
  }

  #PBMac-{{ section.id }} .pbmac-trust p {
    font-size: 11px;
  }

  @media (max-width: 749px) {
    #PBMac-{{ section.id }} .pbmac-title {
      font-size: 29px;
      line-height: 1.16;
    }
  }


  /* V4: lighter title + fallback 18-color picker */
  #PBMac-{{ section.id }},
  #PBMac-{{ section.id }} button,
  #PBMac-{{ section.id }} input,
  #PBMac-{{ section.id }} select,
  #PBMac-{{ section.id }} textarea {
    font-family: "Segoe UI", "Helvetica Neue", Arial, sans-serif !important;
  }

  #PBMac-{{ section.id }} .pbmac-title {
    max-width: 760px;
    color: #2f2d31;
    font-family: "Segoe UI", "Helvetica Neue", Arial, sans-serif !important;
    font-size: clamp(30px, 2.2vw, 38px) !important;
    line-height: 1.19 !important;
    letter-spacing: -.025em !important;
    font-weight: 600 !important;
  }

  #PBMac-{{ section.id }} .pbmac-option__head legend,
  #PBMac-{{ section.id }} .pbmac-personalization__head strong,
  #PBMac-{{ section.id }} .pbmac-accordion summary {
    font-weight: 600 !important;
  }

  #PBMac-{{ section.id }} .pbmac-custom-color-card {
    margin: 2px 0 21px;
    padding: 16px;
    border: 1px solid #ead9c8;
    border-radius: 16px;
    background: #fffdfa;
  }

  #PBMac-{{ section.id }} .pbmac-custom-color-card__head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 14px;
    margin-bottom: 12px;
  }

  #PBMac-{{ section.id }} .pbmac-custom-color-card__head > strong {
    color: #543a2b;
    font-size: 15px;
    font-weight: 650;
  }

  #PBMac-{{ section.id }} .pbmac-custom-color-card__head > span {
    padding: 6px 10px;
    border: 1px solid #ead9c8;
    border-radius: 999px;
    background: #fff8f2;
    color: #9a887c;
    font-size: 12px;
    line-height: 1;
  }

  #PBMac-{{ section.id }} .pbmac-custom-color-card__head b {
    color: #5d473a;
    font-weight: 650;
  }

  #PBMac-{{ section.id }} .pbmac-custom-color-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 9px;
  }

  #PBMac-{{ section.id }} .pbmac-custom-color {
    position: relative;
    min-width: 0;
    cursor: pointer;
  }

  #PBMac-{{ section.id }} .pbmac-custom-color input {
    position: absolute;
    opacity: 0;
    pointer-events: none;
  }

  #PBMac-{{ section.id }} .pbmac-custom-color__control {
    position: relative;
    min-height: 56px;
    width: 100%;
    padding: 9px 34px 9px 10px;
    border: 1px solid #ead9c8;
    border-radius: 13px;
    background: #fffdfa;
    display: flex;
    align-items: center;
    gap: 9px;
    transition: border-color .15s ease, box-shadow .15s ease, background .15s ease;
  }

  #PBMac-{{ section.id }} .pbmac-custom-color:hover .pbmac-custom-color__control {
    border-color: #cfa477;
    background: #fff;
  }

  #PBMac-{{ section.id }} .pbmac-custom-color__dot {
    width: 27px;
    height: 27px;
    flex: 0 0 27px;
    border-radius: 999px;
    border: 1px solid rgba(48, 38, 31, .14);
    box-shadow: inset 0 0 0 1px rgba(255,255,255,.25);
  }

  #PBMac-{{ section.id }} .pbmac-custom-color__name {
    min-width: 0;
    color: #5b483d;
    font-size: 13px;
    line-height: 1.15;
    font-weight: 600;
  }

  #PBMac-{{ section.id }} .pbmac-custom-color__control em {
    display: none;
    position: absolute;
    right: 7px;
    top: 50%;
    width: 20px;
    height: 20px;
    transform: translateY(-50%);
    place-items: center;
    border-radius: 999px;
    background: #c79868;
    color: #fff;
    font-size: 11px;
    line-height: 20px;
    text-align: center;
    font-style: normal;
    font-weight: 800;
  }

  #PBMac-{{ section.id }} .pbmac-custom-color input:checked + .pbmac-custom-color__control {
    border-color: #d28c4b;
    box-shadow: inset 0 0 0 1px #d28c4b;
    background: #fffaf5;
  }

  #PBMac-{{ section.id }} .pbmac-custom-color input:checked + .pbmac-custom-color__control em {
    display: grid;
  }

  @media (max-width: 749px) {
    #PBMac-{{ section.id }} .pbmac-title {
      font-size: 28px !important;
      line-height: 1.2 !important;
    }

    #PBMac-{{ section.id }} .pbmac-custom-color-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }


  /* ==========================================================
     V5 — TOP REVIEW SUMMARY + PRICE + FOOTER NEWSLETTER
     ========================================================== */

  #PBMac-{{ section.id }} .pbmac-rating--manual {
    gap: 7px;
  }

  #PBMac-{{ section.id }} .pbmac-rating__stars {
    display: inline-flex;
    gap: 1px;
    color: #c9c3c6;
    font-size: 13px;
    line-height: 1;
  }

  #PBMac-{{ section.id }} .pbmac-rating__stars i {
    font-style: normal;
  }

  #PBMac-{{ section.id }} .pbmac-rating__stars .is-on {
    color: #2f2930;
  }

  #PBMac-{{ section.id }} .pbmac-rating em {
    color: #6f686c;
    font-size: 11px;
  }

  #PBMac-{{ section.id }} .pbmac-price-row {
    gap: 8px;
    align-items: baseline;
  }

  #PBMac-{{ section.id }} .pbmac-compare {
    order: 1;
    color: #777174 !important;
    font-size: 14px !important;
    font-weight: 500;
    text-decoration: line-through;
    text-decoration-thickness: 1px;
  }

  #PBMac-{{ section.id }} .pbmac-price {
    order: 2;
    color: #f16422 !important;
    font-size: 25px !important;
    line-height: 1;
    font-weight: 750 !important;
    letter-spacing: -.025em;
  }

  #PBMac-{{ section.id }} .pbmac-save {
    order: 3;
    color: #d95b27;
    font-size: 11px !important;
    font-weight: 700;
  }

  .pbmac-newsletter {
    padding: 36px 0 38px;
    background: #e9e3dd;
    color: #1f1b1f;
    font-family: "Segoe UI", "Helvetica Neue", Arial, sans-serif;
  }

  .pbmac-newsletter__inner {
    width: min(100%, 780px);
    margin: 0 auto;
    text-align: center;
  }

  .pbmac-newsletter__copy h2 {
    margin: 0;
    color: #171417;
    font-size: 20px;
    line-height: 1.2;
    font-weight: 700;
    letter-spacing: -.01em;
  }

  .pbmac-newsletter__copy p {
    margin: 7px 0 15px;
    color: #2f2a2d;
    font-size: 13px;
    line-height: 1.45;
    font-weight: 500;
  }

  .pbmac-newsletter__form {
    width: min(100%, 620px);
    margin: 0 auto;
  }

  .pbmac-newsletter__field {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: 12px;
    align-items: center;
  }

  .pbmac-newsletter__field input {
    width: 100%;
    height: 51px;
    padding: 0 17px;
    border: 1px solid #393338;
    border-radius: 999px;
    background: transparent;
    color: #2f292d;
    font: inherit;
    font-size: 13px;
    outline: 0;
  }

  .pbmac-newsletter__field input::placeholder {
    color: #72696d;
    opacity: 1;
  }

  .pbmac-newsletter__field input:focus {
    box-shadow: 0 0 0 2px rgba(47, 41, 45, .08);
  }

  .pbmac-newsletter__field button {
    height: 51px;
    padding: 0 20px;
    border: 0;
    border-radius: 999px;
    background: #302a31;
    color: #fff;
    font: inherit;
    font-size: 12px;
    font-weight: 700;
    cursor: pointer;
  }

  .pbmac-newsletter__field button:hover {
    background: #211d22;
  }

  .pbmac-newsletter__success,
  .pbmac-newsletter__error {
    margin: 10px 0 0;
    font-size: 12px;
  }

  .pbmac-newsletter__success {
    color: #2d6f4d;
  }

  .pbmac-newsletter__error {
    color: #a84a3d;
  }

  @media (max-width: 749px) {
    .pbmac-newsletter {
      padding: 30px 0 32px;
    }

    .pbmac-newsletter__inner {
      width: 100%;
    }

    .pbmac-newsletter__field {
      grid-template-columns: 1fr;
      gap: 9px;
    }

    .pbmac-newsletter__field button {
      width: 100%;
    }
  }


  /* ==========================================================
     V7 — COMPACT COLOR + FALLBACK SIZE + PERSONALIZED CARD
     ========================================================== */

  #PBMac-{{ section.id }} .pbmac-fallback-option {
    margin: 0 0 19px;
    padding: 0;
    border: 0;
    min-width: 0;
  }

  #PBMac-{{ section.id }} .pbmac-fallback-option__head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 14px;
    margin-bottom: 9px;
  }

  #PBMac-{{ section.id }} .pbmac-fallback-option__head legend {
    color: #393438;
    font-size: 13px;
    line-height: 1.2;
    font-weight: 600;
  }

  #PBMac-{{ section.id }} .pbmac-fallback-option__head legend > span {
    margin-left: 5px;
    color: #9a9296;
    font-size: 12px;
    font-weight: 500;
  }

  /* compact color swatches exactly like the reference */
  #PBMac-{{ section.id }} .pbmac-fallback-colors {
    display: flex;
    flex-wrap: wrap;
    gap: 7px;
  }

  #PBMac-{{ section.id }} .pbmac-fallback-color-pill {
    position: relative;
    cursor: pointer;
  }

  #PBMac-{{ section.id }} .pbmac-fallback-color-pill input {
    position: absolute;
    opacity: 0;
    pointer-events: none;
  }

  #PBMac-{{ section.id }} .pbmac-fallback-color-pill__outer {
    width: 56px;
    height: 35px;
    padding: 3px;
    display: block;
    border: 1px solid #ded8d4;
    border-radius: 999px;
    background: #fff;
    transition: border-color .15s ease, box-shadow .15s ease;
  }

  #PBMac-{{ section.id }} .pbmac-fallback-color-pill__outer i {
    width: 100%;
    height: 100%;
    display: block;
    border-radius: 999px;
    border: 1px solid rgba(35, 30, 33, .06);
  }

  #PBMac-{{ section.id }} .pbmac-fallback-color-pill:hover .pbmac-fallback-color-pill__outer {
    border-color: #8f8689;
  }

  #PBMac-{{ section.id }} .pbmac-fallback-color-pill input:checked + .pbmac-fallback-color-pill__outer {
    border-color: #423b40;
    box-shadow: 0 0 0 1px #423b40;
  }

  /* fallback Size pills */
  #PBMac-{{ section.id }} .pbmac-fallback-size-pills {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
  }

  #PBMac-{{ section.id }} .pbmac-fallback-size-pill {
    position: relative;
    cursor: pointer;
  }

  #PBMac-{{ section.id }} .pbmac-fallback-size-pill input {
    position: absolute;
    opacity: 0;
    pointer-events: none;
  }

  #PBMac-{{ section.id }} .pbmac-fallback-size-pill span {
    min-width: 50px;
    min-height: 42px;
    padding: 0 12px;
    border: 1px solid #e2dcd8;
    border-radius: 999px;
    background: #fff;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    color: #777074;
    font-size: 12px;
    font-weight: 600;
    transition: border-color .15s ease, box-shadow .15s ease, color .15s ease;
  }

  #PBMac-{{ section.id }} .pbmac-fallback-size-pill input:checked + span {
    border-color: #403940;
    box-shadow: inset 0 0 0 1px #403940;
    color: #393236;
  }

  /* Personalized card */
  #PBMac-{{ section.id }} .pbmac-personalization-card {
    margin: 6px 0 18px;
    overflow: hidden;
    border-radius: 10px;
    background: #f4efe9;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__banner {
    min-height: 40px;
    padding: 0 13px;
    display: flex;
    align-items: center;
    gap: 8px;
    background: linear-gradient(90deg, #ff7b31 0%, #f59e70 48%, #f4efe9 100%);
    color: #fff;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__banner span {
    font-size: 13px;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__banner strong {
    font-size: 13px;
    line-height: 1;
    font-weight: 700;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__body {
    padding: 13px 13px 14px;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__body > label {
    display: block;
    margin-bottom: 7px;
    color: #3f393d;
    font-size: 12px;
    line-height: 1.2;
    font-weight: 600;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__body label sup {
    color: #e5423c;
    font-size: 12px;
    top: 0;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__input-wrap {
    position: relative;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card .pbmac-input {
    width: 100%;
    height: 47px;
    padding: 0 54px 0 13px;
    border: 1px solid #4b4548;
    border-radius: 7px;
    background: #fff;
    color: #403a3d;
    font-size: 12.5px;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card .pbmac-input::placeholder {
    color: #aca4a8;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__counter {
    position: absolute;
    right: 11px;
    top: 50%;
    transform: translateY(-50%);
    color: #c9c2c5;
    font-size: 10px;
    pointer-events: none;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__counter b {
    font-weight: 500;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__body > p {
    margin: 7px 0 0;
    color: #a79fa3;
    font-size: 10.5px;
    line-height: 1.55;
  }

  @media (max-width: 749px) {
    #PBMac-{{ section.id }} .pbmac-fallback-color-pill__outer {
      width: 52px;
      height: 33px;
    }

    #PBMac-{{ section.id }} .pbmac-fallback-size-pill span {
      min-width: 48px;
      min-height: 40px;
    }
  }


  /* ==========================================================
     V8 — COMPACT PRODUCT INFO, CLOSER TO REFERENCE
     ========================================================== */

  #PBMac-{{ section.id }} .pbmac-info {
    font-family: Arial, "Helvetica Neue", Helvetica, sans-serif !important;
  }

  #PBMac-{{ section.id }} .pbmac-title {
    max-width: 620px;
    margin: 0 !important;
    color: #252226 !important;
    font-family: Arial, "Helvetica Neue", Helvetica, sans-serif !important;
    font-size: clamp(18px, 1.35vw, 22px) !important;
    line-height: 1.36 !important;
    letter-spacing: -.012em !important;
    font-weight: 650 !important;
  }

  #PBMac-{{ section.id }} .pbmac-meta {
    margin-top: 7px !important;
  }

  #PBMac-{{ section.id }} .pbmac-rating,
  #PBMac-{{ section.id }} .pbmac-rating--manual {
    gap: 5px !important;
    font-size: 11px !important;
  }

  #PBMac-{{ section.id }} .pbmac-rating__stars {
    gap: 0 !important;
    font-size: 11.5px !important;
  }

  #PBMac-{{ section.id }} .pbmac-rating__stars .is-on {
    color: #302b30 !important;
  }

  #PBMac-{{ section.id }} .pbmac-rating em {
    color: #777075 !important;
    font-size: 10.5px !important;
  }

  #PBMac-{{ section.id }} .pbmac-price-row {
    margin-top: 9px !important;
    margin-bottom: 20px !important;
    gap: 8px !important;
    align-items: baseline !important;
  }

  #PBMac-{{ section.id }} .pbmac-compare {
    color: #5f595d !important;
    font-size: 13px !important;
    line-height: 1 !important;
    font-weight: 400 !important;
    text-decoration-thickness: 1px !important;
  }

  #PBMac-{{ section.id }} .pbmac-price {
    color: #f16422 !important;
    font-size: 23px !important;
    line-height: 1 !important;
    font-weight: 700 !important;
    letter-spacing: -.015em !important;
  }

  #PBMac-{{ section.id }} .pbmac-save {
    color: #d95b27 !important;
    font-size: 10px !important;
    line-height: 1 !important;
    font-weight: 700 !important;
  }

  #PBMac-{{ section.id }} .pbmac-save--hidden {
    display: none !important;
  }

  #PBMac-{{ section.id }} .pbmac-form {
    margin-top: 0 !important;
  }

  #PBMac-{{ section.id }} .pbmac-option,
  #PBMac-{{ section.id }} .pbmac-fallback-option {
    margin-bottom: 16px !important;
  }

  #PBMac-{{ section.id }} .pbmac-option__head,
  #PBMac-{{ section.id }} .pbmac-fallback-option__head {
    margin-bottom: 7px !important;
  }

  #PBMac-{{ section.id }} .pbmac-option__head legend,
  #PBMac-{{ section.id }} .pbmac-fallback-option__head legend {
    color: #3b3639 !important;
    font-size: 12px !important;
    line-height: 1.2 !important;
    font-weight: 600 !important;
  }

  #PBMac-{{ section.id }} .pbmac-option__selected,
  #PBMac-{{ section.id }} .pbmac-fallback-option__head legend > span {
    color: #948d91 !important;
    font-size: 11px !important;
  }

  #PBMac-{{ section.id }} .pbmac-sizechart-link {
    color: #554e52 !important;
    font-size: 10.5px !important;
  }

  /* Style / native size */
  #PBMac-{{ section.id }} .pbmac-pills {
    gap: 7px !important;
  }

  #PBMac-{{ section.id }} .pbmac-pill span {
    min-width: 54px !important;
    min-height: 38px !important;
    padding: 0 13px !important;
    border: 1px solid #e1dad6 !important;
    border-radius: 999px !important;
    color: #686166 !important;
    background: #fff !important;
    font-size: 11px !important;
    line-height: 1 !important;
    font-weight: 500 !important;
  }

  #PBMac-{{ section.id }} .pbmac-pill input:checked + span {
    border-color: #403a3e !important;
    box-shadow: inset 0 0 0 1px #403a3e !important;
    color: #332e32 !important;
    background: #fff !important;
    font-weight: 600 !important;
  }

  /* native Color */
  #PBMac-{{ section.id }} .pbmac-colors {
    gap: 6px !important;
  }

  #PBMac-{{ section.id }} .pbmac-color__outer {
    width: 50px !important;
    height: 31px !important;
    padding: 3px !important;
    border-color: #ded8d4 !important;
  }

  /* fallback Color */
  #PBMac-{{ section.id }} .pbmac-fallback-colors {
    gap: 6px !important;
  }

  #PBMac-{{ section.id }} .pbmac-fallback-color-pill__outer {
    width: 50px !important;
    height: 31px !important;
    padding: 3px !important;
  }

  /* fallback Size */
  #PBMac-{{ section.id }} .pbmac-fallback-size-pills {
    gap: 7px !important;
  }

  #PBMac-{{ section.id }} .pbmac-fallback-size-pill span {
    min-width: 50px !important;
    min-height: 38px !important;
    padding: 0 11px !important;
    color: #777074 !important;
    font-size: 11px !important;
    font-weight: 500 !important;
  }

  #PBMac-{{ section.id }} .pbmac-fallback-size-pill input:checked + span {
    color: #312c30 !important;
    font-weight: 600 !important;
  }

  /* Personalization stays visually distinct but more compact */
  #PBMac-{{ section.id }} .pbmac-personalization-card {
    margin-top: 5px !important;
    margin-bottom: 15px !important;
    border-radius: 9px !important;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__banner {
    min-height: 36px !important;
    padding: 0 12px !important;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__banner strong {
    font-size: 12px !important;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__body {
    padding: 11px 12px 12px !important;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__body > label {
    margin-bottom: 6px !important;
    font-size: 11px !important;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card .pbmac-input {
    height: 44px !important;
    font-size: 12px !important;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__body > p {
    font-size: 10px !important;
  }

  #PBMac-{{ section.id }} .pbmac-buy-row {
    margin-top: 14px !important;
    grid-template-columns: 110px minmax(0, 1fr) !important;
  }

  #PBMac-{{ section.id }} .pbmac-qty,
  #PBMac-{{ section.id }} .pbmac-preview {
    height: 47px !important;
  }

  #PBMac-{{ section.id }} .pbmac-atc {
    height: 51px !important;
    margin-top: 9px !important;
    font-size: 12.5px !important;
  }

  #PBMac-{{ section.id }} .pbmac-trust {
    margin-top: 11px !important;
  }

  #PBMac-{{ section.id }} .pbmac-trust p {
    margin: 5px 0 !important;
    font-size: 10px !important;
  }

  #PBMac-{{ section.id }} .pbmac-accordions {
    margin-top: 11px !important;
  }

  #PBMac-{{ section.id }} .pbmac-accordion summary {
    min-height: 48px !important;
    font-size: 11.5px !important;
  }

  @media (max-width: 749px) {
    #PBMac-{{ section.id }} .pbmac-title {
      font-size: 18px !important;
      line-height: 1.38 !important;
    }

    #PBMac-{{ section.id }} .pbmac-price {
      font-size: 22px !important;
    }

    #PBMac-{{ section.id }} .pbmac-color__outer,
    #PBMac-{{ section.id }} .pbmac-fallback-color-pill__outer {
      width: 48px !important;
      height: 30px !important;
    }
  }


  /* ==========================================================
     V9 — MACORNER-LIKE TYPOGRAPHY SYSTEM
     Neutral system sans + intentional type/weight hierarchy.
     ========================================================== */

  #PBMac-{{ section.id }},
  #PBMac-{{ section.id }} *,
  .pbmac-reviews,
  .pbmac-reviews *,
  .pbmac-newsletter,
  .pbmac-newsletter * {
    font-family: Arial, Helvetica, sans-serif !important;
  }

  /* PRODUCT TITLE
     Macorner keeps the product title compact and only semi-bold. */
  #PBMac-{{ section.id }} .pbmac-title {
    max-width: 660px !important;
    margin: 0 !important;
    color: #262226 !important;
    font-size: 17px !important;
    line-height: 1.43 !important;
    letter-spacing: -.008em !important;
    font-weight: 600 !important;
  }

  /* Rating is deliberately much lighter/smaller than title. */
  #PBMac-{{ section.id }} .pbmac-meta {
    margin-top: 6px !important;
  }

  #PBMac-{{ section.id }} .pbmac-rating,
  #PBMac-{{ section.id }} .pbmac-rating--manual {
    min-height: 16px;
    gap: 5px !important;
    color: #494348 !important;
    font-size: 11px !important;
    line-height: 1 !important;
    font-weight: 400 !important;
  }

  #PBMac-{{ section.id }} .pbmac-rating__stars {
    display: inline-flex;
    gap: 0 !important;
    font-size: 11px !important;
    line-height: 1 !important;
  }

  #PBMac-{{ section.id }} .pbmac-rating__stars .is-on {
    color: #29242a !important;
  }

  #PBMac-{{ section.id }} .pbmac-rating em {
    color: #6e676b !important;
    font-size: 10.5px !important;
    font-style: normal !important;
    font-weight: 400 !important;
  }

  /* PRICE HIERARCHY */
  #PBMac-{{ section.id }} .pbmac-price-row {
    margin-top: 9px !important;
    margin-bottom: 21px !important;
    gap: 7px !important;
    align-items: baseline !important;
  }

  #PBMac-{{ section.id }} .pbmac-compare {
    color: #625c60 !important;
    font-size: 13px !important;
    line-height: 1 !important;
    font-weight: 400 !important;
    text-decoration-line: line-through !important;
    text-decoration-thickness: 1px !important;
  }

  #PBMac-{{ section.id }} .pbmac-price {
    color: #f16422 !important;
    font-size: 24px !important;
    line-height: 1 !important;
    letter-spacing: -.018em !important;
    font-weight: 700 !important;
  }

  #PBMac-{{ section.id }} .pbmac-save {
    color: #d95d29 !important;
    font-size: 10.5px !important;
    line-height: 1 !important;
    font-weight: 600 !important;
  }

  /* OPTION SECTION LABELS:
     noticeably smaller/lighter than title and price. */
  #PBMac-{{ section.id }} .pbmac-option,
  #PBMac-{{ section.id }} .pbmac-fallback-option {
    margin-bottom: 17px !important;
  }

  #PBMac-{{ section.id }} .pbmac-option__head,
  #PBMac-{{ section.id }} .pbmac-fallback-option__head {
    margin-bottom: 8px !important;
  }

  #PBMac-{{ section.id }} .pbmac-option__head legend,
  #PBMac-{{ section.id }} .pbmac-fallback-option__head legend {
    color: #3a3538 !important;
    font-size: 13px !important;
    line-height: 1.2 !important;
    letter-spacing: 0 !important;
    font-weight: 500 !important;
  }

  #PBMac-{{ section.id }} .pbmac-option__selected,
  #PBMac-{{ section.id }} .pbmac-fallback-option__head legend > span {
    margin-left: 4px !important;
    color: #989094 !important;
    font-size: 12px !important;
    font-weight: 400 !important;
  }

  #PBMac-{{ section.id }} .pbmac-sizechart-link {
    color: #4f484c !important;
    font-size: 11px !important;
    line-height: 1 !important;
    font-weight: 400 !important;
  }

  /* STYLE + NATIVE SIZE PILLS */
  #PBMac-{{ section.id }} .pbmac-pills {
    gap: 8px !important;
  }

  #PBMac-{{ section.id }} .pbmac-pill span {
    min-width: 55px !important;
    min-height: 40px !important;
    padding: 0 14px !important;
    border: 1px solid #e3ddd9 !important;
    border-radius: 999px !important;
    background: #fff !important;
    color: #666064 !important;
    font-size: 12px !important;
    line-height: 1 !important;
    font-weight: 400 !important;
  }

  #PBMac-{{ section.id }} .pbmac-pill input:checked + span {
    border-color: #3a3439 !important;
    box-shadow: inset 0 0 0 1px #3a3439 !important;
    color: #2e292d !important;
    background: #fff !important;
    font-weight: 500 !important;
  }

  /* COLOR SWATCHES */
  #PBMac-{{ section.id }} .pbmac-colors,
  #PBMac-{{ section.id }} .pbmac-fallback-colors {
    gap: 7px !important;
  }

  #PBMac-{{ section.id }} .pbmac-color__outer,
  #PBMac-{{ section.id }} .pbmac-fallback-color-pill__outer {
    width: 52px !important;
    height: 32px !important;
    padding: 3px !important;
    border: 1px solid #ddd7d3 !important;
    border-radius: 999px !important;
    background: #fff !important;
  }

  #PBMac-{{ section.id }} .pbmac-color input:checked + .pbmac-color__outer,
  #PBMac-{{ section.id }} .pbmac-fallback-color-pill input:checked + .pbmac-fallback-color-pill__outer {
    border-color: #3a3439 !important;
    box-shadow: 0 0 0 1px #3a3439 !important;
  }

  /* FALLBACK SIZE */
  #PBMac-{{ section.id }} .pbmac-fallback-size-pills {
    gap: 8px !important;
  }

  #PBMac-{{ section.id }} .pbmac-fallback-size-pill span {
    min-width: 51px !important;
    min-height: 40px !important;
    padding: 0 12px !important;
    border-color: #e3ddd9 !important;
    color: #70696d !important;
    font-size: 12px !important;
    line-height: 1 !important;
    font-weight: 400 !important;
  }

  #PBMac-{{ section.id }} .pbmac-fallback-size-pill input:checked + span {
    border-color: #3a3439 !important;
    box-shadow: inset 0 0 0 1px #3a3439 !important;
    color: #2d282c !important;
    font-weight: 500 !important;
  }

  /* PERSONALIZATION: this is intentionally stronger than options,
     but still less dominant than Add to Cart. */
  #PBMac-{{ section.id }} .pbmac-personalization-card {
    margin-top: 6px !important;
    margin-bottom: 17px !important;
    border-radius: 9px !important;
    background: #f5f1ec !important;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__banner {
    min-height: 38px !important;
    padding: 0 13px !important;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__banner span {
    font-size: 12px !important;
    font-weight: 400 !important;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__banner strong {
    font-size: 13px !important;
    line-height: 1 !important;
    font-weight: 600 !important;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__body {
    padding: 12px 13px 13px !important;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__body > label {
    margin-bottom: 7px !important;
    color: #3e383b !important;
    font-size: 12px !important;
    line-height: 1.2 !important;
    font-weight: 600 !important;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card .pbmac-input {
    height: 47px !important;
    padding-left: 13px !important;
    border-radius: 7px !important;
    color: #3d373b !important;
    font-size: 13px !important;
    font-weight: 400 !important;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__counter {
    font-size: 10px !important;
    font-weight: 400 !important;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__body > p {
    margin-top: 7px !important;
    color: #a1999d !important;
    font-size: 11px !important;
    line-height: 1.45 !important;
    font-weight: 400 !important;
  }

  /* PURCHASE ACTIONS */
  #PBMac-{{ section.id }} .pbmac-buy-row {
    margin-top: 15px !important;
    grid-template-columns: 110px minmax(0, 1fr) !important;
    gap: 10px !important;
  }

  #PBMac-{{ section.id }} .pbmac-qty,
  #PBMac-{{ section.id }} .pbmac-preview {
    height: 48px !important;
  }

  #PBMac-{{ section.id }} .pbmac-qty input {
    font-size: 13px !important;
    font-weight: 600 !important;
  }

  #PBMac-{{ section.id }} .pbmac-preview {
    color: #342e33 !important;
    font-size: 13px !important;
    line-height: 1 !important;
    font-weight: 600 !important;
  }

  #PBMac-{{ section.id }} .pbmac-atc {
    height: 52px !important;
    margin-top: 9px !important;
    font-size: 14px !important;
    line-height: 1 !important;
    font-weight: 700 !important;
  }

  /* TRUST + ACCORDIONS */
  #PBMac-{{ section.id }} .pbmac-trust {
    margin-top: 12px !important;
  }

  #PBMac-{{ section.id }} .pbmac-trust p {
    margin: 5px 0 !important;
    color: #625b5f !important;
    font-size: 11px !important;
    line-height: 1.4 !important;
    font-weight: 400 !important;
  }

  #PBMac-{{ section.id }} .pbmac-accordions {
    margin-top: 12px !important;
  }

  #PBMac-{{ section.id }} .pbmac-accordion summary {
    min-height: 50px !important;
    color: #423c40 !important;
    font-size: 13px !important;
    line-height: 1.2 !important;
    font-weight: 500 !important;
  }

  #PBMac-{{ section.id }} .pbmac-accordion__content {
    color: #71696d !important;
    font-size: 12.5px !important;
    line-height: 1.6 !important;
    font-weight: 400 !important;
  }

  /* REVIEW TYPOGRAPHY */
  .pbmac-review-tabs-top span {
    font-size: 13px !important;
    font-weight: 500 !important;
  }

  .pbmac-reviews-card h2 {
    font-size: 16px !important;
    font-weight: 600 !important;
  }

  .pbmac-review-images-head h3,
  .pbmac-topreviews-head h3 {
    font-size: 20px !important;
    font-weight: 600 !important;
  }

  .pbmac-review__headline strong {
    font-size: 13px !important;
    font-weight: 500 !important;
  }

  .pbmac-review__body {
    font-size: 12.5px !important;
    font-weight: 400 !important;
  }

  /* NEWSLETTER TYPOGRAPHY */
  .pbmac-newsletter__copy h2 {
    font-size: 20px !important;
    font-weight: 600 !important;
  }

  .pbmac-newsletter__copy p {
    font-size: 13px !important;
    font-weight: 400 !important;
  }

  @media (max-width: 749px) {
    #PBMac-{{ section.id }} .pbmac-title {
      font-size: 16px !important;
      line-height: 1.45 !important;
    }

    #PBMac-{{ section.id }} .pbmac-price {
      font-size: 23px !important;
    }

    #PBMac-{{ section.id }} .pbmac-pill span,
    #PBMac-{{ section.id }} .pbmac-fallback-size-pill span {
      min-height: 39px !important;
    }
  }


  /* ==========================================================
     V10 — REVIEW TYPOGRAPHY MATCH
     Tuned to the supplied Macorner review reference.
     ========================================================== */

  .pbmac-reviews,
  .pbmac-reviews button,
  .pbmac-reviews input,
  .pbmac-reviews textarea,
  .pbmac-reviews select {
    font-family: Arial, Helvetica, sans-serif !important;
  }

  /* top Product Reviews tab */
  .pbmac-review-tabs-top span {
    color: #373236 !important;
    font-size: 13px !important;
    line-height: 1.2 !important;
    font-weight: 500 !important;
    letter-spacing: 0 !important;
  }

  /* left summary card */
  .pbmac-reviews-card h2 {
    margin-bottom: 21px !important;
    color: #302c2f !important;
    font-family: Arial, Helvetica, sans-serif !important;
    font-size: 15.5px !important;
    line-height: 1.25 !important;
    font-weight: 600 !important;
    letter-spacing: 0 !important;
  }

  .pbmac-reviews-card__score > strong {
    color: #382d33 !important;
    font-family: Georgia, "Times New Roman", serif !important;
    font-size: 48px !important;
    line-height: .98 !important;
    font-weight: 400 !important;
    letter-spacing: -.03em !important;
  }

  .pbmac-reviews-card__stars {
    color: #f16422 !important;
    font-family: Arial, Helvetica, sans-serif !important;
    font-size: 19px !important;
    line-height: 1 !important;
    letter-spacing: 1.5px !important;
    font-weight: 400 !important;
  }

  .pbmac-reviews-card__score small {
    color: #4f484c !important;
    font-family: Arial, Helvetica, sans-serif !important;
    font-size: 12px !important;
    line-height: 1.2 !important;
    font-weight: 400 !important;
  }

  .pbmac-reviews-card .pbmac-histogram__row {
    color: #514b4e !important;
    font-family: Arial, Helvetica, sans-serif !important;
    font-size: 11.5px !important;
    line-height: 1 !important;
    font-weight: 400 !important;
  }

  .pbmac-reviews-card .pbmac-histogram__row b {
    color: #82797d !important;
    font-size: 11px !important;
    font-weight: 400 !important;
  }

  .pbmac-review-static-button {
    color: #3f393d !important;
    font-family: Arial, Helvetica, sans-serif !important;
    font-size: 12px !important;
    line-height: 1 !important;
    font-weight: 500 !important;
  }

  /* right column headings */
  .pbmac-review-images-head h3,
  .pbmac-topreviews-head h3 {
    color: #322d31 !important;
    font-family: Arial, Helvetica, sans-serif !important;
    font-size: 20px !important;
    line-height: 1.2 !important;
    font-weight: 600 !important;
    letter-spacing: 0 !important;
  }

  /* star filter chips */
  .pbmac-review-filters button {
    color: #6d6569 !important;
    font-family: Arial, Helvetica, sans-serif !important;
    font-size: 11.5px !important;
    line-height: 1 !important;
    font-weight: 400 !important;
  }

  .pbmac-review-filters button.is-active {
    color: #383236 !important;
    font-weight: 500 !important;
  }

  /* each review */
  .pbmac-review__stars {
    font-family: Arial, Helvetica, sans-serif !important;
    font-size: 13.5px !important;
    line-height: 1 !important;
    letter-spacing: .2px !important;
  }

  .pbmac-review__headline strong {
    color: #3e383c !important;
    font-family: Arial, Helvetica, sans-serif !important;
    font-size: 12.5px !important;
    line-height: 1.2 !important;
    font-weight: 500 !important;
  }

  .pbmac-review__meta {
    color: #9c9498 !important;
    font-family: Arial, Helvetica, sans-serif !important;
    font-size: 10.5px !important;
    line-height: 1.25 !important;
    font-weight: 400 !important;
  }

  .pbmac-review h4 {
    color: #423b3f !important;
    font-family: Arial, Helvetica, sans-serif !important;
    font-size: 13px !important;
    line-height: 1.35 !important;
    font-weight: 500 !important;
  }

  .pbmac-review__body,
  .pbmac-review-modal-source__details .rte {
    color: #514a4e !important;
    font-family: Arial, Helvetica, sans-serif !important;
    font-size: 12.5px !important;
    line-height: 1.52 !important;
    font-weight: 400 !important;
    letter-spacing: 0 !important;
  }

  /* modal typography */
  .pbmac-review-modal-source__details strong {
    color: #3d373b !important;
    font-family: Arial, Helvetica, sans-serif !important;
    font-size: 13px !important;
    font-weight: 500 !important;
  }

  .pbmac-review-modal-source__details h4 {
    color: #3f393d !important;
    font-family: Arial, Helvetica, sans-serif !important;
    font-size: 14px !important;
    line-height: 1.35 !important;
    font-weight: 500 !important;
  }

  /* Mobile keeps the same hierarchy but slightly tighter */
  @media (max-width: 749px) {
    .pbmac-reviews-card h2 {
      font-size: 15px !important;
    }

    .pbmac-reviews-card__score > strong {
      font-size: 46px !important;
    }

    .pbmac-review-images-head h3,
    .pbmac-topreviews-head h3 {
      font-size: 19px !important;
    }

    .pbmac-review__body {
      font-size: 12px !important;
    }
  }


  /* ==========================================================
     V11 — REVIEW UI RESET / REFERENCE MATCH
     This block intentionally wins over ALL earlier review styles.
     ========================================================== */

  .pbmac-reviews,
  .pbmac-reviews *,
  .pbmac-review-modal,
  .pbmac-review-modal * {
    font-family: Arial, Helvetica, sans-serif !important;
    box-sizing: border-box;
  }

  .pbmac-reviews {
    padding: 26px 0 42px !important;
    background: #fbf9f7 !important;
    color: #373236 !important;
  }

  /* PRODUCT REVIEWS TAB */
  .pbmac-review-tabs-top {
    width: min(100%, 1180px) !important;
    margin: 0 auto 17px !important;
    border-bottom: 1px solid #e5dfda !important;
  }

  .pbmac-review-tabs-top span {
    position: relative !important;
    display: inline-flex !important;
    padding: 0 0 12px !important;
    color: #322d31 !important;
    font-size: 13px !important;
    line-height: 1 !important;
    font-weight: 500 !important;
  }

  .pbmac-review-tabs-top span::after {
    content: '' !important;
    position: absolute !important;
    left: 0 !important;
    right: 0 !important;
    bottom: -1px !important;
    height: 2px !important;
    background: #3b3539 !important;
  }

  /* MAIN 2-COLUMN REVIEW LAYOUT */
  .pbmac-reviews-layout {
    width: min(100%, 1180px) !important;
    margin: 0 auto !important;
    display: grid !important;
    grid-template-columns: 320px minmax(0, 1fr) !important;
    gap: 42px !important;
    align-items: start !important;
  }

  /* LEFT SUMMARY BOX */
  .pbmac-reviews-card {
    padding: 25px 22px 24px !important;
    border: 0 !important;
    border-radius: 8px !important;
    background: #f3f0ec !important;
    box-shadow: none !important;
  }

  .pbmac-reviews-card h2 {
    margin: 0 0 22px !important;
    color: #332f32 !important;
    font-size: 15px !important;
    line-height: 1.2 !important;
    font-weight: 600 !important;
  }

  .pbmac-reviews-card__score {
    position: relative !important;
    display: grid !important;
    grid-template-columns: 56px 1px minmax(0, 1fr) !important;
    gap: 12px !important;
    align-items: center !important;
    margin: 0 0 23px !important;
  }

  /* vertical divider between 4.9 and stars */
  .pbmac-reviews-card__score::before {
    content: '' !important;
    width: 1px !important;
    height: 38px !important;
    background: #d8d0cb !important;
    grid-column: 2 !important;
    grid-row: 1 !important;
  }

  .pbmac-reviews-card__score > strong {
    grid-column: 1 !important;
    grid-row: 1 !important;
    color: #3b2e35 !important;
    font-family: Georgia, "Times New Roman", serif !important;
    font-size: 48px !important;
    line-height: .92 !important;
    font-weight: 400 !important;
    letter-spacing: -.035em !important;
  }

  .pbmac-reviews-card__score > div {
    grid-column: 3 !important;
    grid-row: 1 !important;
    display: grid !important;
    gap: 4px !important;
    align-content: center !important;
  }

  .pbmac-reviews-card__stars {
    color: #f16422 !important;
    font-size: 18px !important;
    line-height: 1 !important;
    letter-spacing: 1.4px !important;
    white-space: nowrap !important;
  }

  .pbmac-reviews-card__score small {
    color: #454045 !important;
    font-size: 12px !important;
    line-height: 1.1 !important;
    font-weight: 400 !important;
  }

  /* HISTOGRAM */
  .pbmac-reviews-card .pbmac-histogram {
    display: grid !important;
    gap: 13px !important;
    margin: 0 !important;
  }

  .pbmac-reviews-card .pbmac-histogram__row {
    display: grid !important;
    grid-template-columns: 38px minmax(0, 1fr) 30px !important;
    gap: 9px !important;
    align-items: center !important;
    min-height: 14px !important;
    color: #514b4e !important;
    font-size: 11px !important;
    line-height: 1 !important;
    font-weight: 400 !important;
  }

  .pbmac-reviews-card .pbmac-histogram__row > div {
    height: 7px !important;
    overflow: hidden !important;
    border-radius: 999px !important;
    background: #e4ded8 !important;
  }

  .pbmac-reviews-card .pbmac-histogram__row i {
    display: block !important;
    height: 100% !important;
    border-radius: inherit !important;
    background: #ff7040 !important;
  }

  .pbmac-reviews-card .pbmac-histogram__row b {
    color: #81797d !important;
    text-align: right !important;
    font-size: 10.5px !important;
    line-height: 1 !important;
    font-weight: 400 !important;
  }

  .pbmac-review-static-button {
    min-height: 42px !important;
    margin: 21px 0 0 !important;
    padding: 0 14px !important;
    border: 1px solid #3f393d !important;
    border-radius: 999px !important;
    display: inline-flex !important;
    align-items: center !important;
    justify-content: center !important;
    background: transparent !important;
    color: #403a3e !important;
    font-size: 11.5px !important;
    line-height: 1 !important;
    font-weight: 500 !important;
  }

  /* RIGHT COLUMN */
  .pbmac-reviews-content {
    min-width: 0 !important;
  }

  .pbmac-review-images-head,
  .pbmac-topreviews-head {
    display: flex !important;
    align-items: center !important;
    justify-content: space-between !important;
  }

  .pbmac-review-images-head h3,
  .pbmac-topreviews-head h3 {
    margin: 0 !important;
    color: #302b2f !important;
    font-size: 20px !important;
    line-height: 1.15 !important;
    font-weight: 600 !important;
  }

  .pbmac-review-images-arrows {
    display: flex !important;
    gap: 7px !important;
  }

  .pbmac-review-images-arrows button {
    width: 30px !important;
    height: 30px !important;
    padding: 0 !important;
    border: 0 !important;
    background: transparent !important;
    color: #443e42 !important;
    font-size: 26px !important;
    line-height: 1 !important;
    cursor: pointer !important;
  }

  /* IMAGE STRIP */
  .pbmac-review-images {
    display: flex !important;
    gap: 8px !important;
    margin: 15px 0 0 !important;
    padding: 0 0 2px !important;
    overflow-x: auto !important;
    scroll-behavior: smooth !important;
    scrollbar-width: none !important;
  }

  .pbmac-review-images::-webkit-scrollbar {
    display: none !important;
  }

  .pbmac-review-image-card {
    width: 166px !important;
    flex: 0 0 166px !important;
    aspect-ratio: 1 / 1 !important;
    padding: 0 !important;
    overflow: hidden !important;
    border: 0 !important;
    border-radius: 7px !important;
    background: #f0eeec !important;
    cursor: pointer !important;
  }

  .pbmac-review-image-card__image {
    width: 100% !important;
    height: 100% !important;
    display: block !important;
    object-fit: cover !important;
  }

  .pbmac-topreviews-head {
    margin: 31px 0 12px !important;
  }

  /* FILTERS */
  .pbmac-review-filters {
    display: flex !important;
    flex-wrap: wrap !important;
    gap: 7px !important;
    margin: 0 0 7px !important;
  }

  .pbmac-review-filters button {
    min-height: 36px !important;
    padding: 0 13px !important;
    border: 1px solid #e5ddd7 !important;
    border-radius: 999px !important;
    background: #fff !important;
    color: #70676c !important;
    font-size: 11px !important;
    line-height: 1 !important;
    font-weight: 400 !important;
    cursor: pointer !important;
  }

  .pbmac-review-filters button.is-active {
    border-color: #3d373b !important;
    box-shadow: inset 0 0 0 1px #3d373b !important;
    color: #332d31 !important;
    font-weight: 500 !important;
  }

  .pbmac-review-filters button:disabled {
    opacity: .32 !important;
    cursor: not-allowed !important;
  }

  /* REVIEW ROWS */
  .pbmac-review {
    padding: 16px 0 !important;
    border-bottom: 1px solid #eee6e1 !important;
    background: transparent !important;
    cursor: pointer !important;
  }

  .pbmac-review[hidden] {
    display: none !important;
  }

  .pbmac-review:hover {
    background: transparent !important;
  }

  .pbmac-review__headline {
    display: flex !important;
    align-items: center !important;
    gap: 7px !important;
  }

  /* CRITICAL STAR FIX:
     old theme used var(--orange), which was undefined. */
  .pbmac-review__stars {
    display: inline-flex !important;
    gap: 0 !important;
    color: #d8d2ce !important;
    font-size: 13px !important;
    line-height: 1 !important;
    letter-spacing: .15px !important;
  }

  .pbmac-review__stars i {
    color: #d8d2ce !important;
    font-style: normal !important;
  }

  .pbmac-review__stars i.is-on,
  .pbmac-review__stars .is-on {
    color: #f16422 !important;
  }

  .pbmac-review__headline strong {
    color: #3b3539 !important;
    font-size: 12.5px !important;
    line-height: 1.1 !important;
    font-weight: 500 !important;
  }

  .pbmac-review__meta {
    display: flex !important;
    align-items: center !important;
    gap: 7px !important;
    margin: 6px 0 0 !important;
    color: #a0979b !important;
    font-size: 10.5px !important;
    line-height: 1.1 !important;
    font-weight: 400 !important;
  }

  .pbmac-review h4 {
    margin: 9px 0 4px !important;
    color: #423c40 !important;
    font-size: 13px !important;
    line-height: 1.35 !important;
    font-weight: 500 !important;
  }

  .pbmac-review__body {
    color: #544d51 !important;
    font-size: 12.5px !important;
    line-height: 1.48 !important;
    font-weight: 400 !important;
  }

  .pbmac-review__body > :first-child,
  .pbmac-review__body > :last-child {
    margin-top: 0 !important;
    margin-bottom: 0 !important;
  }

  .pbmac-review__thumb {
    width: 70px !important;
    height: 70px !important;
    margin: 9px 0 0 !important;
    border: 1px solid #e2dad5 !important;
    border-radius: 7px !important;
    object-fit: cover !important;
  }

  /* PAGINATION */
  .pbmac-pagination {
    display: flex !important;
    gap: 5px !important;
    margin: 14px 0 0 !important;
  }

  .pbmac-pagination button {
    min-width: 31px !important;
    height: 31px !important;
    padding: 0 8px !important;
    border: 1px solid #e5ddd7 !important;
    border-radius: 999px !important;
    background: #fff !important;
    color: #766d72 !important;
    font-size: 11px !important;
    font-weight: 400 !important;
  }

  .pbmac-pagination button.is-active {
    border-color: #4b4348 !important;
    background: #4b4348 !important;
    color: #fff !important;
  }

  /* REVIEW MODAL STARS — FIXED TOO */
  .pbmac-review-modal .pbmac-review__stars i {
    color: #d8d2ce !important;
  }

  .pbmac-review-modal .pbmac-review__stars i.is-on,
  .pbmac-review-modal .pbmac-review__stars .is-on {
    color: #f16422 !important;
  }

  /* MOBILE — MATCH THE USER'S REFERENCE SCREENSHOT */
  @media (max-width: 749px) {
    .pbmac-reviews {
      padding: 0 0 34px !important;
      background: #fbf9f7 !important;
    }

    .pbmac-review-tabs-top {
      width: 100% !important;
      margin: 0 0 13px !important;
      padding: 0 !important;
    }

    .pbmac-review-tabs-top span {
      margin-left: 0 !important;
      font-size: 12px !important;
    }

    .pbmac-reviews-layout {
      width: 100% !important;
      display: block !important;
      margin: 0 !important;
    }

    .pbmac-reviews-card {
      width: 100% !important;
      margin: 0 0 18px !important;
      padding: 25px 16px 21px !important;
      border-radius: 8px !important;
    }

    .pbmac-reviews-card h2 {
      margin-bottom: 23px !important;
      font-size: 14px !important;
    }

    .pbmac-reviews-card__score {
      grid-template-columns: 52px 1px minmax(0, 1fr) !important;
      gap: 11px !important;
      margin-bottom: 23px !important;
    }

    .pbmac-reviews-card__score > strong {
      font-size: 46px !important;
    }

    .pbmac-reviews-card__stars {
      font-size: 18px !important;
    }

    .pbmac-reviews-card__score small {
      font-size: 11.5px !important;
    }

    .pbmac-reviews-card .pbmac-histogram {
      gap: 13px !important;
    }

    .pbmac-reviews-card .pbmac-histogram__row {
      grid-template-columns: 36px minmax(0, 1fr) 29px !important;
      gap: 8px !important;
      font-size: 10.5px !important;
    }

    .pbmac-review-static-button {
      min-height: 41px !important;
      margin-top: 21px !important;
      font-size: 11px !important;
    }

    .pbmac-review-images-head,
    .pbmac-topreviews-head,
    .pbmac-review-filters,
    .pbmac-review-list,
    .pbmac-pagination {
      padding-left: 0 !important;
      padding-right: 0 !important;
    }

    .pbmac-review-images-head h3,
    .pbmac-topreviews-head h3 {
      font-size: 18px !important;
    }

    .pbmac-review-images {
      width: 100vw !important;
      max-width: 100vw !important;
      margin-left: calc(50% - 50vw) !important;
      padding-left: 0 !important;
      gap: 7px !important;
    }

    .pbmac-review-image-card {
      width: 88px !important;
      flex-basis: 88px !important;
      height: 92px !important;
      aspect-ratio: auto !important;
      border-radius: 6px !important;
    }

    .pbmac-review-images-arrows {
      display: none !important;
    }

    .pbmac-topreviews-head {
      margin-top: 29px !important;
      margin-bottom: 12px !important;
    }

    .pbmac-review-filters {
      width: 100vw !important;
      max-width: 100vw !important;
      margin-left: calc(50% - 50vw) !important;
      padding: 0 0 4px !important;
      flex-wrap: nowrap !important;
      overflow-x: auto !important;
      scrollbar-width: none !important;
    }

    .pbmac-review-filters::-webkit-scrollbar {
      display: none !important;
    }

    .pbmac-review-filters button {
      flex: 0 0 auto !important;
      min-height: 34px !important;
      padding: 0 11px !important;
      font-size: 10.5px !important;
    }

    .pbmac-review {
      padding: 15px 0 !important;
    }

    .pbmac-review__body {
      font-size: 12px !important;
    }
  }


  /* ==========================================================
     V12 — FIX REVIEW IMAGE STRIP OVERFLOW
     Keep the image carousel inside the review content on mobile.
     ========================================================== */

  .pbmac-reviews-content {
    min-width: 0 !important;
    max-width: 100% !important;
    overflow: hidden !important;
  }

  .pbmac-review-images-head {
    width: 100% !important;
    max-width: 100% !important;
  }

  .pbmac-review-images {
    width: 100% !important;
    max-width: 100% !important;
    margin-left: 0 !important;
    margin-right: 0 !important;
    padding: 0 2px 5px 0 !important;
    overflow-x: auto !important;
    overflow-y: hidden !important;
    overscroll-behavior-x: contain;
    scroll-snap-type: x proximity;
    scroll-padding-inline: 0;
  }

  .pbmac-review-image-card {
    scroll-snap-align: start;
  }

  @media (max-width: 749px) {
    .pbmac-reviews-content {
      width: 100% !important;
      max-width: 100% !important;
      min-width: 0 !important;
      overflow: hidden !important;
    }

    .pbmac-review-images-head,
    .pbmac-topreviews-head {
      width: 100% !important;
      max-width: 100% !important;
      padding-left: 0 !important;
      padding-right: 0 !important;
    }

    .pbmac-review-images {
      width: 100% !important;
      max-width: 100% !important;
      margin-left: 0 !important;
      margin-right: 0 !important;
      padding: 0 3px 5px 0 !important;
      gap: 8px !important;
      overflow-x: auto !important;
      overflow-y: hidden !important;
      scrollbar-width: none !important;
      -webkit-overflow-scrolling: touch;
    }

    .pbmac-review-images::-webkit-scrollbar {
      display: none !important;
    }

    .pbmac-review-image-card {
      width: 84px !important;
      height: 84px !important;
      flex: 0 0 84px !important;
      aspect-ratio: 1 / 1 !important;
      border-radius: 7px !important;
      overflow: hidden !important;
    }

    .pbmac-review-image-card__image {
      width: 100% !important;
      height: 100% !important;
      object-fit: cover !important;
      display: block !important;
    }

    /* Keep filters in their own contained horizontal scroller too. */
    .pbmac-review-filters {
      width: 100% !important;
      max-width: 100% !important;
      margin-left: 0 !important;
      margin-right: 0 !important;
      padding: 0 0 4px !important;
      overflow-x: auto !important;
      overflow-y: hidden !important;
      flex-wrap: nowrap !important;
      scrollbar-width: none !important;
      -webkit-overflow-scrolling: touch;
    }

    .pbmac-review-filters::-webkit-scrollbar {
      display: none !important;
    }
  }


  /* ==========================================================
     V13 — PERSONALIZED SPARKLES + MORE ITEMS TO CONSIDER
     ========================================================== */

  /* 3 sparkle stars in Personalized banner */
  #PBMac-{{ section.id }} .pbmac-personalization-card__sparkles {
    position: relative;
    width: 16px;
    min-width: 16px;
    height: 14px;
    display: inline-block;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__sparkles .pbmac-sparkle {
    position: absolute;
    color: #fff9f3;
    font-style: normal;
    line-height: 1;
    transform-origin: center;
    animation: pbmacSparkleTwinkle 1.9s ease-in-out infinite;
    opacity: .95;
    text-shadow: 0 0 8px rgba(255,255,255,.24);
  }

  #PBMac-{{ section.id }} .pbmac-sparkle--1 {
    left: 0;
    top: 5px;
    font-size: 9px;
    animation-delay: 0s;
  }

  #PBMac-{{ section.id }} .pbmac-sparkle--2 {
    left: 7px;
    top: 0;
    font-size: 12px;
    animation-delay: .25s;
  }

  #PBMac-{{ section.id }} .pbmac-sparkle--3 {
    left: 11px;
    top: 8px;
    font-size: 7px;
    animation-delay: .5s;
  }

  @keyframes pbmacSparkleTwinkle {
    0%, 100% {
      opacity: .65;
      transform: scale(.8);
      filter: brightness(.95);
    }
    35% {
      opacity: 1;
      transform: scale(1.1);
      filter: brightness(1.12);
    }
    60% {
      opacity: .82;
      transform: scale(.92);
      filter: brightness(1);
    }
  }

  /* More Items to Consider */
  .pbmac-more-items {
    padding: 12px 0 18px;
    background: #fbf9f7;
  }

  .pbmac-more-items__inner h2 {
    margin: 0 0 14px;
    color: #363136;
    font-family: Arial, Helvetica, sans-serif;
    font-size: 18px;
    line-height: 1.25;
    font-weight: 500;
  }

  .pbmac-more-items__grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 18px;
  }

  .pbmac-more-card {
    display: block;
    color: inherit;
    text-decoration: none;
  }

  .pbmac-more-card__media {
    overflow: hidden;
    border-radius: 8px;
    background: #f3eee9;
    aspect-ratio: 1 / 1;
  }

  .pbmac-more-card__image,
  .pbmac-more-card__image--placeholder {
    width: 100%;
    height: 100%;
    display: block;
    object-fit: cover;
  }

  .pbmac-more-card__content {
    padding-top: 9px;
  }

  .pbmac-more-card h3 {
    margin: 0 0 7px;
    color: #373236;
    font-family: Arial, Helvetica, sans-serif;
    font-size: 14px;
    line-height: 1.35;
    font-weight: 500;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
    min-height: calc(1.35em * 2);
  }

  .pbmac-more-card__stars {
    margin: 0 0 6px;
    color: #f16422;
    font-size: 15px;
    line-height: 1;
    letter-spacing: 1px;
  }

  .pbmac-more-card__price {
    display: flex;
    align-items: baseline;
    gap: 6px;
    flex-wrap: wrap;
  }

  .pbmac-more-card__sale {
    color: #f16422;
    font-size: 14px;
    line-height: 1;
    font-weight: 600;
  }

  .pbmac-more-card__compare {
    color: #7d767a;
    font-size: 12px;
    line-height: 1;
    font-weight: 400;
    text-decoration: line-through;
    text-decoration-thickness: 1px;
  }

  @media (max-width: 989px) {
    .pbmac-more-items__grid {
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 15px;
    }
  }

  @media (max-width: 749px) {
    .pbmac-more-items {
      padding: 10px 0 14px;
    }

    .pbmac-more-items__inner h2 {
      margin-bottom: 12px;
      font-size: 17px;
      font-weight: 500;
    }

    .pbmac-more-items__grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 16px 14px;
    }

    .pbmac-more-card__content {
      padding-top: 8px;
    }

    .pbmac-more-card h3 {
      font-size: 13px;
      line-height: 1.32;
      min-height: calc(1.32em * 2);
      margin-bottom: 6px;
    }

    .pbmac-more-card__stars {
      font-size: 14px;
      letter-spacing: .8px;
      margin-bottom: 6px;
    }

    .pbmac-more-card__sale {
      font-size: 13px;
    }

    .pbmac-more-card__compare {
      font-size: 11px;
    }
  }


  /* ==========================================================
     V14 — COMPACT VERTICAL RHYTHM
     Tightens the whole purchase column so each element visually
     belongs to the next, matching the competitor reference.
     ========================================================== */

  #PBMac-{{ section.id }} .pbmac-info {
    font-family: Arial, Helvetica, sans-serif !important;
  }

  /* TITLE → RATING → PRICE */
  #PBMac-{{ section.id }} .pbmac-title {
    margin: 0 !important;
    max-width: 650px !important;
    color: #272327 !important;
    font-size: 17px !important;
    line-height: 1.42 !important;
    letter-spacing: -.008em !important;
    font-weight: 600 !important;
  }

  #PBMac-{{ section.id }} .pbmac-meta {
    margin: 5px 0 0 !important;
    min-height: 15px !important;
  }

  #PBMac-{{ section.id }} .pbmac-rating,
  #PBMac-{{ section.id }} .pbmac-rating--manual {
    min-height: 15px !important;
    gap: 4px !important;
    line-height: 1 !important;
  }

  #PBMac-{{ section.id }} .pbmac-price-row {
    margin: 8px 0 14px !important;
    gap: 7px !important;
    min-height: 27px !important;
    align-items: baseline !important;
  }

  #PBMac-{{ section.id }} .pbmac-compare {
    font-size: 12.5px !important;
  }

  #PBMac-{{ section.id }} .pbmac-price {
    font-size: 23px !important;
  }

  #PBMac-{{ section.id }} .pbmac-save {
    font-size: 9.5px !important;
  }

  /* FORM STARTS IMMEDIATELY AFTER PRICE */
  #PBMac-{{ section.id }} .pbmac-form {
    margin: 0 !important;
    padding: 0 !important;
  }

  /* ALL OPTIONS SHARE THE SAME RHYTHM */
  #PBMac-{{ section.id }} .pbmac-option,
  #PBMac-{{ section.id }} .pbmac-fallback-option {
    margin: 0 0 13px !important;
    padding: 0 !important;
  }

  #PBMac-{{ section.id }} .pbmac-option__head,
  #PBMac-{{ section.id }} .pbmac-fallback-option__head {
    min-height: 16px !important;
    margin: 0 0 6px !important;
  }

  #PBMac-{{ section.id }} .pbmac-option__head legend,
  #PBMac-{{ section.id }} .pbmac-fallback-option__head legend {
    margin: 0 !important;
    font-size: 12px !important;
    line-height: 1.15 !important;
    font-weight: 500 !important;
  }

  #PBMac-{{ section.id }} .pbmac-option__selected,
  #PBMac-{{ section.id }} .pbmac-fallback-option__head legend > span {
    font-size: 11px !important;
    font-weight: 400 !important;
  }

  #PBMac-{{ section.id }} .pbmac-sizechart-link {
    margin: 0 !important;
    font-size: 10.5px !important;
    line-height: 1.15 !important;
  }

  /* COLOR ROWS */
  #PBMac-{{ section.id }} .pbmac-colors,
  #PBMac-{{ section.id }} .pbmac-fallback-colors {
    gap: 6px !important;
    row-gap: 6px !important;
  }

  #PBMac-{{ section.id }} .pbmac-color__outer,
  #PBMac-{{ section.id }} .pbmac-fallback-color-pill__outer {
    width: 50px !important;
    height: 31px !important;
  }

  /* SIZE / STYLE */
  #PBMac-{{ section.id }} .pbmac-pills,
  #PBMac-{{ section.id }} .pbmac-fallback-size-pills {
    gap: 7px !important;
    row-gap: 7px !important;
  }

  #PBMac-{{ section.id }} .pbmac-pill span,
  #PBMac-{{ section.id }} .pbmac-fallback-size-pill span {
    min-height: 39px !important;
  }

  /* SIZE SHOULD FLOW DIRECTLY INTO PERSONALIZED */
  #PBMac-{{ section.id }} .pbmac-fallback-size,
  #PBMac-{{ section.id }} .pbmac-option:has(legend:first-child) {
    margin-bottom: 13px !important;
  }

  /* PERSONALIZED */
  #PBMac-{{ section.id }} .pbmac-personalization-card {
    margin: 1px 0 11px !important;
    border-radius: 9px !important;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__banner {
    min-height: 36px !important;
    padding: 0 12px !important;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__banner strong {
    font-size: 12.5px !important;
    font-weight: 600 !important;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__body {
    padding: 10px 12px 11px !important;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__body > label {
    margin: 0 0 6px !important;
    font-size: 11px !important;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card .pbmac-input {
    height: 43px !important;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__body > p {
    margin: 6px 0 0 !important;
    font-size: 9.8px !important;
    line-height: 1.4 !important;
  }

  /* PERSONALIZED → QUANTITY/PREVIEW → ADD TO CART */
  #PBMac-{{ section.id }} .pbmac-buy-row {
    margin: 0 !important;
    gap: 9px !important;
    grid-template-columns: 110px minmax(0, 1fr) !important;
  }

  #PBMac-{{ section.id }} .pbmac-qty,
  #PBMac-{{ section.id }} .pbmac-preview {
    height: 47px !important;
  }

  #PBMac-{{ section.id }} .pbmac-atc {
    height: 51px !important;
    margin: 8px 0 0 !important;
  }

  /* BUY AREA → TRUST */
  #PBMac-{{ section.id }} .pbmac-trust {
    margin: 9px 0 0 !important;
    padding: 0 2px !important;
  }

  #PBMac-{{ section.id }} .pbmac-trust p {
    margin: 4px 0 !important;
    font-size: 10px !important;
    line-height: 1.3 !important;
  }

  /* TRUST → ACCORDIONS */
  #PBMac-{{ section.id }} .pbmac-accordions {
    margin: 11px 0 0 !important;
  }

  #PBMac-{{ section.id }} .pbmac-accordion summary {
    min-height: 48px !important;
    padding: 0 !important;
    font-size: 11.5px !important;
  }

  #PBMac-{{ section.id }} .pbmac-accordion__content {
    padding: 0 2px 13px !important;
  }

  /* Keep product-info content visually grouped. */
  #PBMac-{{ section.id }} .pbmac-info > *:first-child {
    margin-top: 0 !important;
  }

  /* MOBILE REFERENCE RHYTHM */
  @media (max-width: 749px) {
    #PBMac-{{ section.id }} .pbmac-info {
      margin-top: 18px !important;
    }

    #PBMac-{{ section.id }} .pbmac-title {
      font-size: 16px !important;
      line-height: 1.43 !important;
    }

    #PBMac-{{ section.id }} .pbmac-meta {
      margin-top: 5px !important;
    }

    #PBMac-{{ section.id }} .pbmac-price-row {
      margin-top: 8px !important;
      margin-bottom: 13px !important;
    }

    #PBMac-{{ section.id }} .pbmac-option,
    #PBMac-{{ section.id }} .pbmac-fallback-option {
      margin-bottom: 12px !important;
    }

    #PBMac-{{ section.id }} .pbmac-personalization-card {
      margin-top: 0 !important;
      margin-bottom: 10px !important;
    }

    #PBMac-{{ section.id }} .pbmac-trust {
      margin-top: 8px !important;
    }

    #PBMac-{{ section.id }} .pbmac-accordions {
      margin-top: 10px !important;
    }
  }


  /* ==========================================================
     V15 — READABLE MACORNER SCALE
     V14 spacing retained; typography and buy hierarchy enlarged.
     ========================================================== */

  #PBMac-{{ section.id }},
  #PBMac-{{ section.id }} button,
  #PBMac-{{ section.id }} input,
  #PBMac-{{ section.id }} select,
  #PBMac-{{ section.id }} textarea {
    font-family: Arial, Helvetica, sans-serif !important;
  }

  /* Personalization */
  #PBMac-{{ section.id }} .pbmac-personalization-card {
    margin-bottom: 15px !important;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__banner {
    min-height: 40px !important;
    padding: 0 13px !important;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__banner strong {
    font-size: 14px !important;
    line-height: 1 !important;
    font-weight: 700 !important;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__sparkles {
    width: 18px !important;
    min-width: 18px !important;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__body {
    padding: 13px 13px 14px !important;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__body > label {
    margin-bottom: 8px !important;
    color: #393438 !important;
    font-size: 13px !important;
    line-height: 1.2 !important;
    font-weight: 600 !important;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card .pbmac-input {
    height: 48px !important;
    padding: 0 55px 0 13px !important;
    color: #3c373a !important;
    font-size: 14px !important;
    line-height: 1 !important;
    font-weight: 400 !important;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card .pbmac-input::placeholder {
    color: #aca5a8 !important;
    font-size: 14px !important;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__counter {
    right: 12px !important;
    font-size: 10.5px !important;
  }

  #PBMac-{{ section.id }} .pbmac-personalization-card__body > p {
    margin-top: 8px !important;
    color: #a19a9e !important;
    font-size: 12px !important;
    line-height: 1.48 !important;
    font-weight: 400 !important;
  }

  /* Quantity gets its own row like Macorner */
  #PBMac-{{ section.id }} .pbmac-quantity-block {
    margin: 0 0 12px !important;
  }

  #PBMac-{{ section.id }} .pbmac-quantity-label {
    display: block !important;
    margin: 0 0 8px !important;
    color: #3a3538 !important;
    font-size: 13px !important;
    line-height: 1.2 !important;
    font-weight: 500 !important;
  }

  #PBMac-{{ section.id }} .pbmac-qty {
    width: 112px !important;
    height: 46px !important;
    grid-template-columns: 36px 1fr 36px !important;
    border-color: #e2dcd8 !important;
  }

  #PBMac-{{ section.id }} .pbmac-qty button {
    color: #4c4649 !important;
    font-size: 20px !important;
    font-weight: 400 !important;
  }

  #PBMac-{{ section.id }} .pbmac-qty input {
    color: #3a3538 !important;
    font-size: 14px !important;
    line-height: 1 !important;
    font-weight: 500 !important;
  }

  /* Preview is full width, easy to scan */
  #PBMac-{{ section.id }} .pbmac-preview--full {
    width: 100% !important;
    height: 49px !important;
    margin: 0 0 10px !important;
    border: 1.2px solid #3c363a !important;
    border-radius: 999px !important;
    display: flex !important;
    align-items: center !important;
    justify-content: center !important;
    gap: 8px !important;
    background: #fff !important;
    color: #373136 !important;
    font-size: 14px !important;
    line-height: 1 !important;
    font-weight: 600 !important;
  }

  #PBMac-{{ section.id }} .pbmac-button-icon {
    width: 18px !important;
    height: 18px !important;
    flex: 0 0 18px !important;
    display: block !important;
  }

  #PBMac-{{ section.id }} .pbmac-button-icon--eye {
    width: 17px !important;
    height: 17px !important;
  }

  /* Add to Cart */
  #PBMac-{{ section.id }} .pbmac-atc {
    width: 100% !important;
    height: 53px !important;
    margin: 0 !important;
    border-radius: 999px !important;
    display: flex !important;
    align-items: center !important;
    justify-content: center !important;
    gap: 9px !important;
    font-size: 15px !important;
    line-height: 1 !important;
    font-weight: 700 !important;
  }

  /* Trust messages */
  #PBMac-{{ section.id }} .pbmac-trust {
    margin: 15px 0 0 !important;
    padding: 0 2px !important;
  }

  #PBMac-{{ section.id }} .pbmac-trust p {
    margin: 8px 0 !important;
    display: flex !important;
    align-items: flex-start !important;
    gap: 8px !important;
    color: #5d565a !important;
    font-size: 12.5px !important;
    line-height: 1.4 !important;
    font-weight: 400 !important;
  }

  #PBMac-{{ section.id }} .pbmac-trust__check {
    flex: 0 0 auto !important;
    color: #4f76ff !important;
    font-size: 16px !important;
    line-height: 1 !important;
    font-weight: 700 !important;
    margin-top: 1px !important;
  }

  /* Accordions: larger, clearer, Macorner-like open pill */
  #PBMac-{{ section.id }} .pbmac-accordions {
    margin-top: 17px !important;
    border-top: 0 !important;
  }

  #PBMac-{{ section.id }} .pbmac-accordion {
    border-bottom: 1px solid #e5dfdb !important;
    padding: 0 !important;
  }

  #PBMac-{{ section.id }} .pbmac-accordion summary {
    min-height: 54px !important;
    padding: 0 10px !important;
    grid-template-columns: 23px minmax(0, 1fr) 20px !important;
    gap: 8px !important;
    color: #50494d !important;
    font-size: 13.5px !important;
    line-height: 1.2 !important;
    font-weight: 500 !important;
    border-radius: 999px !important;
  }

  #PBMac-{{ section.id }} .pbmac-accordion summary > span:first-child {
    font-size: 15px !important;
  }

  #PBMac-{{ section.id }} .pbmac-accordion summary b {
    font-size: 15px !important;
    line-height: 1 !important;
    font-weight: 400 !important;
    transition: transform .18s ease !important;
    transform-origin: center !important;
  }

  #PBMac-{{ section.id }} .pbmac-accordion[open] summary {
    margin: 6px 0 10px !important;
    padding-left: 14px !important;
    padding-right: 14px !important;
    background: #e9e5e1 !important;
    color: #413b3f !important;
  }

  #PBMac-{{ section.id }} .pbmac-accordion[open] summary b {
    transform: rotate(180deg) !important;
  }

  #PBMac-{{ section.id }} .pbmac-accordion__content {
    padding: 4px 12px 18px !important;
    color: #5d565a !important;
    font-size: 14px !important;
    line-height: 1.7 !important;
    font-weight: 400 !important;
  }

  #PBMac-{{ section.id }} .pbmac-accordion__content p {
    margin-top: 0 !important;
    margin-bottom: 12px !important;
  }

  /* Mobile tuned to the exact reference scale */
  @media (max-width: 749px) {
    #PBMac-{{ section.id }} .pbmac-personalization-card__banner strong {
      font-size: 14px !important;
    }

    #PBMac-{{ section.id }} .pbmac-personalization-card__body > label {
      font-size: 13px !important;
    }

    #PBMac-{{ section.id }} .pbmac-personalization-card .pbmac-input,
    #PBMac-{{ section.id }} .pbmac-personalization-card .pbmac-input::placeholder {
      font-size: 14px !important;
    }

    #PBMac-{{ section.id }} .pbmac-personalization-card__body > p {
      font-size: 12px !important;
    }

    #PBMac-{{ section.id }} .pbmac-quantity-label {
      font-size: 13px !important;
    }

    #PBMac-{{ section.id }} .pbmac-preview--full {
      height: 50px !important;
      font-size: 14.5px !important;
    }

    #PBMac-{{ section.id }} .pbmac-atc {
      height: 52px !important;
      font-size: 15.5px !important;
    }

    #PBMac-{{ section.id }} .pbmac-trust p {
      font-size: 12.5px !important;
    }

    #PBMac-{{ section.id }} .pbmac-accordion summary {
      min-height: 55px !important;
      font-size: 13.5px !important;
    }

    #PBMac-{{ section.id }} .pbmac-accordion__content {
      font-size: 14px !important;
      line-height: 1.72 !important;
    }
  }


  /* ==========================================================
     V16 — MOBILE READABILITY
     Match Macorner's readable mobile scale without reopening
     the large vertical gaps from older versions.
     ========================================================== */

  @media (max-width: 749px) {
    /* Product title / price / options */
    #PBMac-{{ section.id }} .pbmac-title {
      font-size: 18px !important;
      line-height: 1.4 !important;
      font-weight: 600 !important;
    }

    #PBMac-{{ section.id }} .pbmac-rating,
    #PBMac-{{ section.id }} .pbmac-rating--manual {
      font-size: 12px !important;
    }

    #PBMac-{{ section.id }} .pbmac-rating__stars {
      font-size: 12px !important;
    }

    #PBMac-{{ section.id }} .pbmac-rating em {
      font-size: 11px !important;
    }

    #PBMac-{{ section.id }} .pbmac-compare {
      font-size: 13.5px !important;
    }

    #PBMac-{{ section.id }} .pbmac-price {
      font-size: 25px !important;
    }

    #PBMac-{{ section.id }} .pbmac-save {
      font-size: 10.5px !important;
    }

    #PBMac-{{ section.id }} .pbmac-option__head legend,
    #PBMac-{{ section.id }} .pbmac-fallback-option__head legend {
      font-size: 14px !important;
      font-weight: 500 !important;
    }

    #PBMac-{{ section.id }} .pbmac-option__selected,
    #PBMac-{{ section.id }} .pbmac-fallback-option__head legend > span {
      font-size: 12.5px !important;
    }

    #PBMac-{{ section.id }} .pbmac-sizechart-link {
      font-size: 12px !important;
    }

    #PBMac-{{ section.id }} .pbmac-pill span,
    #PBMac-{{ section.id }} .pbmac-fallback-size-pill span {
      min-height: 42px !important;
      font-size: 13px !important;
    }

    /* Personalized card */
    #PBMac-{{ section.id }} .pbmac-personalization-card {
      margin-bottom: 16px !important;
    }

    #PBMac-{{ section.id }} .pbmac-personalization-card__banner {
      min-height: 42px !important;
      padding: 0 14px !important;
    }

    #PBMac-{{ section.id }} .pbmac-personalization-card__banner strong {
      font-size: 15px !important;
      font-weight: 700 !important;
    }

    #PBMac-{{ section.id }} .pbmac-personalization-card__body {
      padding: 14px 13px 15px !important;
    }

    #PBMac-{{ section.id }} .pbmac-personalization-card__body > label {
      margin-bottom: 8px !important;
      font-size: 14px !important;
      font-weight: 600 !important;
    }

    #PBMac-{{ section.id }} .pbmac-personalization-card .pbmac-input {
      height: 50px !important;
      font-size: 15px !important;
    }

    #PBMac-{{ section.id }} .pbmac-personalization-card .pbmac-input::placeholder {
      font-size: 15px !important;
    }

    #PBMac-{{ section.id }} .pbmac-personalization-card__counter {
      font-size: 11.5px !important;
    }

    #PBMac-{{ section.id }} .pbmac-personalization-card__body > p {
      margin-top: 8px !important;
      font-size: 13px !important;
      line-height: 1.45 !important;
    }

    /* Quantity */
    #PBMac-{{ section.id }} .pbmac-quantity-block {
      margin-bottom: 13px !important;
    }

    #PBMac-{{ section.id }} .pbmac-quantity-label {
      margin-bottom: 8px !important;
      font-size: 14px !important;
      font-weight: 500 !important;
    }

    #PBMac-{{ section.id }} .pbmac-qty {
      width: 114px !important;
      height: 47px !important;
    }

    #PBMac-{{ section.id }} .pbmac-qty button {
      font-size: 21px !important;
    }

    #PBMac-{{ section.id }} .pbmac-qty input {
      font-size: 15px !important;
      font-weight: 500 !important;
    }

    /* Preview / Add to Cart */
    #PBMac-{{ section.id }} .pbmac-preview--full {
      height: 51px !important;
      margin-bottom: 11px !important;
      font-size: 16px !important;
      font-weight: 600 !important;
    }

    #PBMac-{{ section.id }} .pbmac-button-icon--eye {
      width: 18px !important;
      height: 18px !important;
    }

    #PBMac-{{ section.id }} .pbmac-atc {
      height: 54px !important;
      font-size: 16px !important;
      font-weight: 700 !important;
    }

    #PBMac-{{ section.id }} .pbmac-button-icon--cart {
      width: 19px !important;
      height: 19px !important;
    }

    /* Trust lines */
    #PBMac-{{ section.id }} .pbmac-trust {
      margin-top: 16px !important;
    }

    #PBMac-{{ section.id }} .pbmac-trust p {
      margin: 8px 0 !important;
      gap: 8px !important;
      color: #514b4f !important;
      font-size: 13.5px !important;
      line-height: 1.4 !important;
    }

    #PBMac-{{ section.id }} .pbmac-trust__check {
      font-size: 17px !important;
    }

    /* Accordions */
    #PBMac-{{ section.id }} .pbmac-accordions {
      margin-top: 17px !important;
    }

    #PBMac-{{ section.id }} .pbmac-accordion summary {
      min-height: 57px !important;
      padding-left: 11px !important;
      padding-right: 11px !important;
      font-size: 15px !important;
      line-height: 1.2 !important;
      font-weight: 500 !important;
    }

    #PBMac-{{ section.id }} .pbmac-accordion summary > span:first-child {
      font-size: 16px !important;
    }

    #PBMac-{{ section.id }} .pbmac-accordion summary b {
      font-size: 16px !important;
    }

    #PBMac-{{ section.id }} .pbmac-accordion[open] summary {
      margin-bottom: 12px !important;
      padding-left: 15px !important;
      padding-right: 15px !important;
    }

    #PBMac-{{ section.id }} .pbmac-accordion__content {
      padding: 3px 13px 20px !important;
      color: #554f53 !important;
      font-size: 15px !important;
      line-height: 1.68 !important;
    }

    #PBMac-{{ section.id }} .pbmac-accordion__content p {
      margin-bottom: 13px !important;
    }
  }


  /* ==========================================================
     V17 — DARKER ICONS + BIGGER RATING STARS + TIGHTER SPACING
     ========================================================== */

  /* Dark accordion icons like Macorner */
  #PBMac-{{ section.id }} .pbmac-accordion summary > span:first-child,
  #PBMac-{{ section.id }} .pbmac-accordion summary i,
  #PBMac-{{ section.id }} .pbmac-accordion summary svg {
    color: #201c20 !important;
    fill: #201c20 !important;
    stroke: #201c20 !important;
    opacity: 1 !important;
  }

  /* Also make general tiny UI icons darker */
  #PBMac-{{ section.id }} .pbmac-button-icon,
  #PBMac-{{ section.id }} .pbmac-sizechart-link svg,
  #PBMac-{{ section.id }} .pbmac-rating svg {
    color: #201c20 !important;
    fill: currentColor !important;
    stroke: currentColor !important;
  }

  /* Tighter title/rating/price block */
  #PBMac-{{ section.id }} .pbmac-title {
    margin-bottom: 0 !important;
  }

  #PBMac-{{ section.id }} .pbmac-meta {
    margin-top: 3px !important;
    min-height: auto !important;
    line-height: 1 !important;
  }

  #PBMac-{{ section.id }} .pbmac-price-row {
    margin-top: 6px !important;
    margin-bottom: 11px !important;
    gap: 6px !important;
  }

  /* Product title rating row: bigger stars, darker color */
  #PBMac-{{ section.id }} .pbmac-rating,
  #PBMac-{{ section.id }} .pbmac-rating--manual {
    display: inline-flex !important;
    align-items: center !important;
    gap: 4px !important;
    color: #61595e !important;
    font-size: 12px !important;
    line-height: 1 !important;
    text-decoration: none !important;
  }

  #PBMac-{{ section.id }} .pbmac-rating__stars {
    display: inline-flex !important;
    align-items: center !important;
    gap: 0 !important;
    letter-spacing: .35px !important;
    font-size: 14px !important;
    line-height: 1 !important;
    color: #d8d0cd !important;
  }

  #PBMac-{{ section.id }} .pbmac-rating__stars i,
  #PBMac-{{ section.id }} .pbmac-rating__stars span {
    font-style: normal !important;
  }

  #PBMac-{{ section.id }} .pbmac-rating__stars .is-on {
    color: #1f1b20 !important;
  }

  #PBMac-{{ section.id }} .pbmac-rating em {
    color: #746d72 !important;
    font-size: 12px !important;
    line-height: 1 !important;
    font-style: normal !important;
  }

  /* Make compare / price feel closer like reference */
  #PBMac-{{ section.id }} .pbmac-compare {
    font-size: 13px !important;
  }

  #PBMac-{{ section.id }} .pbmac-price {
    font-size: 24px !important;
    line-height: 1 !important;
  }

  #PBMac-{{ section.id }} .pbmac-save {
    font-size: 11px !important;
    line-height: 1 !important;
  }

  /* Mobile-specific tightening and scale */
  @media (max-width: 749px) {
    #PBMac-{{ section.id }} .pbmac-title {
      font-size: 17px !important;
      line-height: 1.38 !important;
      font-weight: 600 !important;
      margin-bottom: 0 !important;
    }

    #PBMac-{{ section.id }} .pbmac-meta {
      margin-top: 4px !important;
    }

    #PBMac-{{ section.id }} .pbmac-rating,
    #PBMac-{{ section.id }} .pbmac-rating--manual {
      gap: 4px !important;
      font-size: 12px !important;
    }

    #PBMac-{{ section.id }} .pbmac-rating__stars {
      font-size: 15px !important;
      letter-spacing: .45px !important;
    }

    #PBMac-{{ section.id }} .pbmac-rating em {
      font-size: 12px !important;
    }

    #PBMac-{{ section.id }} .pbmac-price-row {
      margin-top: 7px !important;
      margin-bottom: 10px !important;
      gap: 6px !important;
    }

    #PBMac-{{ section.id }} .pbmac-compare {
      font-size: 13.5px !important;
    }

    #PBMac-{{ section.id }} .pbmac-price {
      font-size: 24px !important;
    }

    #PBMac-{{ section.id }} .pbmac-save {
      font-size: 11px !important;
    }

    /* Dark, clear accordion icons and labels */
    #PBMac-{{ section.id }} .pbmac-accordion summary {
      min-height: 54px !important;
      font-size: 15px !important;
      color: #312c30 !important;
    }

    #PBMac-{{ section.id }} .pbmac-accordion summary > span:first-child {
      font-size: 16px !important;
      color: #211d21 !important;
    }
  }


  /* ==========================================================
     V18 — BLACK FILLED ACCORDION ICONS + ACCENT RATING STARS
     ========================================================== */

  #PBMac-{{ section.id }} .pbmac-accordion__icon {
    width: 18px !important;
    height: 18px !important;
    display: inline-flex !important;
    align-items: center !important;
    justify-content: center !important;
    color: #2d292d !important;
    flex: 0 0 18px !important;
  }

  #PBMac-{{ section.id }} .pbmac-accordion__icon svg {
    width: 17px !important;
    height: 17px !important;
    display: block !important;
    color: #2d292d !important;
    fill: currentColor !important;
  }

  #PBMac-{{ section.id }} .pbmac-accordion summary > span:first-child {
    color: #2d292d !important;
    opacity: 1 !important;
  }

  /* Product rating stars use the same orange accent as price / CTA */
  #PBMac-{{ section.id }} .pbmac-rating__stars {
    color: #f16422 !important;
  }

  #PBMac-{{ section.id }} .pbmac-rating__stars i,
  #PBMac-{{ section.id }} .pbmac-rating__stars span {
    color: #d8d0cd !important;
  }

  #PBMac-{{ section.id }} .pbmac-rating__stars .is-on {
    color: #f16422 !important;
  }

  /* Fallback rating text stars, if used */
  #PBMac-{{ section.id }} .pbmac-rating > span:not(.pbmac-rating__stars) {
    color: #f16422 !important;
  }

  @media (max-width: 749px) {
    #PBMac-{{ section.id }} .pbmac-accordion__icon {
      width: 19px !important;
      height: 19px !important;
      flex-basis: 19px !important;
    }

    #PBMac-{{ section.id }} .pbmac-accordion__icon svg {
      width: 18px !important;
      height: 18px !important;
    }

    #PBMac-{{ section.id }} .pbmac-rating__stars {
      font-size: 15px !important;
    }
  }


  /* ==========================================================
     V19 — REMOVE PREVIEW / ADD CUSTOMIZATION SUMMARY + INLINE CTA
     Layout like the reference, but colors remain aligned to this theme.
     ========================================================== */
  #PBMac-{{ section.id }} .pbmac-preview,
  #PBMac-{{ section.id }} [data-pbmac-preview-open],
  #PBMac-{{ section.id }} [data-pbmac-preview-modal] {
    display: none !important;
  }

  #PBMac-{{ section.id }} .pbmac-customization-summary {
    margin: 2px 0 14px !important;
    padding: 12px 14px !important;
    border: 1px solid #ead7c5 !important;
    border-radius: 14px !important;
    background: #fffaf6 !important;
    display: grid !important;
    grid-template-columns: 28px minmax(0,1fr) !important;
    gap: 10px !important;
    align-items: start !important;
  }

  #PBMac-{{ section.id }} .pbmac-customization-summary__icon {
    width: 28px !important;
    height: 28px !important;
    border-radius: 999px !important;
    background: #d77c3d !important;
    color: #fff !important;
    display: grid !important;
    place-items: center !important;
    font-size: 15px !important;
    font-weight: 700 !important;
    line-height: 1 !important;
  }

  #PBMac-{{ section.id }} .pbmac-customization-summary__content strong {
    display: block !important;
    margin: 0 0 4px !important;
    color: #54423a !important;
    font-size: 14px !important;
    line-height: 1.2 !important;
    font-weight: 700 !important;
  }

  #PBMac-{{ section.id }} .pbmac-customization-summary__content span {
    display: block !important;
    color: #8b7669 !important;
    font-size: 13px !important;
    line-height: 1.35 !important;
    font-weight: 500 !important;
  }

  #PBMac-{{ section.id }} .pbmac-customization-summary__content b {
    color: #d77c3d !important;
    font-weight: 600 !important;
  }

  #PBMac-{{ section.id }} .pbmac-customization-summary__content em {
    color: #8b7669 !important;
    font-style: normal !important;
    font-weight: 500 !important;
  }

  #PBMac-{{ section.id }} .pbmac-buy-row--compact {
    display: grid !important;
    grid-template-columns: 124px minmax(0,1fr) !important;
    gap: 12px !important;
    align-items: stretch !important;
    margin: 0 !important;
  }

  #PBMac-{{ section.id }} .pbmac-buy-row--compact .pbmac-qty {
    width: 100% !important;
    height: 50px !important;
    border-color: #dfd6d0 !important;
    background: #fff !important;
  }

  #PBMac-{{ section.id }} .pbmac-atc--inline {
    width: 100% !important;
    height: 50px !important;
    margin: 0 !important;
    border-radius: 14px !important;
    background: #d86e35 !important;
    color: #fff !important;
    display: inline-flex !important;
    align-items: center !important;
    justify-content: center !important;
    gap: 10px !important;
    font-size: 15px !important;
    line-height: 1 !important;
    font-weight: 800 !important;
    text-transform: uppercase !important;
    letter-spacing: .02em !important;
  }

  #PBMac-{{ section.id }} .pbmac-atc--inline:disabled {
    opacity: .45 !important;
  }

  #PBMac-{{ section.id }} .pbmac-atc--inline .pbmac-atc__arrow {
    font-size: 18px !important;
    line-height: 1 !important;
    transform: translateY(-.5px);
  }

  /* Quantity label removed for this layout */
  #PBMac-{{ section.id }} .pbmac-quantity-block,
  #PBMac-{{ section.id }} .pbmac-quantity-label {
    display: none !important;
  }

  @media (max-width: 749px) {
    #PBMac-{{ section.id }} .pbmac-customization-summary {
      margin-bottom: 14px !important;
      padding: 12px 13px !important;
      grid-template-columns: 27px minmax(0,1fr) !important;
      gap: 10px !important;
      border-radius: 14px !important;
    }

    #PBMac-{{ section.id }} .pbmac-customization-summary__icon {
      width: 27px !important;
      height: 27px !important;
      font-size: 14px !important;
    }

    #PBMac-{{ section.id }} .pbmac-customization-summary__content strong {
      font-size: 14px !important;
      margin-bottom: 4px !important;
    }

    #PBMac-{{ section.id }} .pbmac-customization-summary__content span {
      font-size: 13px !important;
    }

    #PBMac-{{ section.id }} .pbmac-buy-row--compact {
      grid-template-columns: 122px minmax(0,1fr) !important;
      gap: 12px !important;
    }

    #PBMac-{{ section.id }} .pbmac-buy-row--compact .pbmac-qty {
      height: 50px !important;
    }

    #PBMac-{{ section.id }} .pbmac-atc--inline {
      height: 50px !important;
      border-radius: 14px !important;
      font-size: 14px !important;
    }
  }


  /* ==========================================================
     V20 — SMALLER PRICE / MONTSERRAT CTA / FIXED CHEVRONS
     ========================================================== */

  /* Price: slightly less dominant */
  #PBMac-{{ section.id }} .pbmac-price {
    font-size: 22px !important;
    line-height: 1 !important;
    font-weight: 700 !important;
    letter-spacing: -.015em !important;
  }

  #PBMac-{{ section.id }} .pbmac-compare {
    font-size: 12.5px !important;
  }

  #PBMac-{{ section.id }} .pbmac-save {
    font-size: 10px !important;
  }

  /* CTA typography: softer / more polished */
  #PBMac-{{ section.id }} .pbmac-atc,
  #PBMac-{{ section.id }} .pbmac-atc--inline,
  #PBMac-{{ section.id }} .pbmac-atc span,
  #PBMac-{{ section.id }} [data-pbmac-atc-text] {
    font-family: "Montserrat", Arial, Helvetica, sans-serif !important;
  }

  #PBMac-{{ section.id }} .pbmac-atc--inline {
    font-size: 14px !important;
    font-weight: 700 !important;
    letter-spacing: 0 !important;
    text-transform: none !important;
  }

  #PBMac-{{ section.id }} .pbmac-atc__arrow {
    font-family: Arial, Helvetica, sans-serif !important;
    font-size: 17px !important;
    font-weight: 400 !important;
  }

  /* Replace the rotating text glyph with a stable SVG chevron */
  #PBMac-{{ section.id }} .pbmac-accordion summary b {
    display: none !important;
  }

  #PBMac-{{ section.id }} .pbmac-accordion__chevron {
    width: 18px !important;
    height: 18px !important;
    display: inline-flex !important;
    align-items: center !important;
    justify-content: center !important;
    color: #342f33 !important;
    transform: none !important;
    transition: none !important;
  }

  #PBMac-{{ section.id }} .pbmac-accordion__chevron svg {
    width: 16px !important;
    height: 16px !important;
    display: block !important;
    fill: none !important;
    stroke: currentColor !important;
    transform: none !important;
  }

  #PBMac-{{ section.id }} .pbmac-accordion[open] .pbmac-accordion__chevron,
  #PBMac-{{ section.id }} .pbmac-accordion[open] .pbmac-accordion__chevron svg {
    transform: none !important;
  }

  /* Existing old rotate rules cannot affect the new SVG */
  #PBMac-{{ section.id }} .pbmac-accordion[open] summary b {
    transform: none !important;
  }

  @media (max-width: 749px) {
    #PBMac-{{ section.id }} .pbmac-price {
      font-size: 21px !important;
    }

    #PBMac-{{ section.id }} .pbmac-compare {
      font-size: 12.5px !important;
    }

    #PBMac-{{ section.id }} .pbmac-atc--inline {
      font-size: 14px !important;
      font-weight: 700 !important;
    }

    #PBMac-{{ section.id }} .pbmac-accordion__chevron {
      width: 18px !important;
      height: 18px !important;
    }

    #PBMac-{{ section.id }} .pbmac-accordion__chevron svg {
      width: 15px !important;
      height: 15px !important;
    }
  }

</style>

<script>
(() => {
  const root=document.getElementById('PBMac-{{ section.id }}'); if(!root||root.dataset.ready==='true')return; root.dataset.ready='true';
  const variants=[{% for variant in product.variants %}{id:{{ variant.id | json }},options:{{ variant.options | json }},available:{{ variant.available | json }},price:{{ variant.price | money_with_currency | json }},compare:{% if variant.compare_at_price > variant.price %}{{ variant.compare_at_price | money_with_currency | json }}{% else %}""{% endif %},save:{% if variant.compare_at_price > variant.price %}{{ variant.compare_at_price | minus: variant.price | times:100 | divided_by:variant.compare_at_price }}{% else %}0{% endif %},mediaId:{% if variant.featured_media %}{{ variant.featured_media.id | json }}{% else %}null{% endif %}}{% unless forloop.last %},{% endunless %}{% endfor %}];
  const slides=[...root.querySelectorAll('[data-pbmac-media]')],thumbs=[...root.querySelectorAll('[data-pbmac-thumb]')];let active=0;const show=(i)=>{if(!slides.length)return;active=(i+slides.length)%slides.length;slides.forEach((s,x)=>{s.hidden=x!==active;s.classList.toggle('is-active',x===active)});thumbs.forEach((t,x)=>t.classList.toggle('is-active',x===active))};thumbs.forEach(t=>t.addEventListener('click',()=>show(Number(t.dataset.pbmacThumb))));root.querySelector('[data-pbmac-prev]')?.addEventListener('click',()=>show(active-1));root.querySelector('[data-pbmac-next]')?.addEventListener('click',()=>show(active+1));
  const byMedia=(id)=>{if(!id)return;const i=slides.findIndex(s=>String(s.dataset.pbmacMediaId)===String(id));if(i>=0)show(i)};
  const groups=[...root.querySelectorAll('[data-pbmac-option-index]')],variantId=root.querySelector('[data-pbmac-variant-id]'),price=root.querySelector('[data-pbmac-price]'),compare=root.querySelector('[data-pbmac-compare]'),save=root.querySelector('[data-pbmac-save]'),atc=root.querySelector('[data-pbmac-atc]'),atcText=root.querySelector('[data-pbmac-atc-text]'),addText={{ section.settings.add_to_cart_text | json }},soldText={{ section.settings.sold_out_text | json }};
  const initialVariantId={{ current_variant.id | json }};

  const defaultVariant=()=>{
    return variants.find(v=>String(v.id)===String(initialVariantId))
      || variants.find(v=>v.available)
      || variants[0]
      || null;
  };

  const selected=()=>groups.map(g=>g.querySelector('[data-pbmac-option-value]:checked')?.value||'');

  const find=()=>{
    // Products with only Shopify's "Default Title" do not render native option groups.
    // Use the selected/first available variant instead of treating the product as unavailable.
    if(!groups.length)return defaultVariant();

    const w=selected();
    if(w.some(value=>!value))return null;

    return variants.find(v=>
      v.options.length===w.length &&
      v.options.every((x,i)=>x===w[i])
    ) || null;
  };

  const render=(v)=>{
    // Extra safeguard for simple products.
    if(!v && !groups.length)v=defaultVariant();

    if(!v){
      if(variantId)variantId.value='';
      if(atc)atc.disabled=true;
      if(atcText)atcText.textContent='UNAVAILABLE';
      return;
    }

    if(variantId)variantId.value=v.id;
    if(price)price.textContent=v.price;

    if(compare){
      compare.textContent=v.compare||'';
      compare.classList.toggle('is-hidden',!v.compare);
    }

    if(save){
      save.textContent=`Save ${v.save}%`;
      save.classList.toggle('is-hidden',!v.save);
    }

    if(atc)atc.disabled=!v.available;
    if(atcText)atcText.textContent=v.available?addText:soldText;

    byMedia(v.mediaId);

    try{
      const u=new URL(location.href);
      u.searchParams.set('variant',v.id);
      history.replaceState({},'',u);
    }catch(e){}
  };const colorSelectedLabel=root.querySelector('[data-pbmac-color-selected]');
  const customColorSelectedLabel=root.querySelector('[data-pbmac-custom-color-selected]');
  const customColorInputs=[...root.querySelectorAll('[data-pbmac-custom-color]')];
  const syncColorSelected=()=>{const s=root.querySelector('[data-pbmac-color-value]:checked');if(colorSelectedLabel&&s)colorSelectedLabel.textContent=s.value;const c=customColorInputs.find(i=>i.checked);if(customColorSelectedLabel&&c)customColorSelectedLabel.textContent=c.value};
  root.querySelectorAll('[data-pbmac-option-value]').forEach(i=>i.addEventListener('change',()=>{render(find());syncColorSelected()}));
  customColorInputs.forEach(i=>i.addEventListener('change',syncColorSelected));
  syncColorSelected();render(find());

  // Final cart safeguard: never submit an empty variant id on a simple product.
  const productForm=root.querySelector('#{{ product_form_id }}');
  productForm?.addEventListener('submit',(event)=>{
    if(variantId && !variantId.value){
      const fallback=defaultVariant();
      if(fallback && fallback.available){
        variantId.value=fallback.id;
        render(fallback);
      }else{
        event.preventDefault();
        if(atcText)atcText.textContent=soldText;
      }
    }
  });

  const qty=root.querySelector('[data-pbmac-qty]'),clamp=v=>Math.min(99,Math.max(1,parseInt(v||'1',10)||1));root.querySelector('[data-pbmac-minus]')?.addEventListener('click',()=>qty.value=clamp(Number(qty.value)-1));root.querySelector('[data-pbmac-plus]')?.addEventListener('click',()=>qty.value=clamp(Number(qty.value)+1));
  const sizeModal=root.querySelector('[data-pbmac-sizechart]');root.querySelector('[data-pbmac-sizechart-open]')?.addEventListener('click',()=>{sizeModal.hidden=false;document.body.classList.add('pbmac-modal-open')});root.querySelectorAll('[data-pbmac-sizechart-close]').forEach(x=>x.addEventListener('click',()=>{sizeModal.hidden=true;document.body.classList.remove('pbmac-modal-open')}));
  const pers=root.querySelector('[data-pbmac-personalization]');
  const persCount=root.querySelector('[data-pbmac-personalization-count]');
  const summaryColor=root.querySelector('[data-pbmac-summary-color]');
  const summaryText=root.querySelector('[data-pbmac-summary-text]');
  const summaryEmpty={{ section.settings.customization_summary_empty | json }};
  const optionValueByName=(needle)=>{
    const names=groups.map(g=>g.querySelector('legend')?.textContent?.trim().toLowerCase()||'');
    const opts=selected();
    const i=names.findIndex(x=>x.includes(needle));
    return i>=0?opts[i]:'';
  };
  const syncPersCount=()=>{if(pers&&persCount)persCount.textContent=pers.value.length};
  const syncSummary=()=>{
    if(summaryColor){
      const nativeColor=optionValueByName('color');
      const customColor=root.querySelector('[data-pbmac-custom-color]:checked')?.value||'';
      const fallbackColor={{ section.settings.default_color_label | json }};
      summaryColor.textContent=(customColor||nativeColor||fallbackColor||'—');
    }
    if(summaryText){
      const txt=(pers?.value||'').trim();
      summaryText.textContent=txt||summaryEmpty;
    }
  };
  pers?.addEventListener('input',()=>{syncPersCount();syncSummary()});
  root.querySelectorAll('[data-pbmac-option-value],[data-pbmac-custom-color],[data-pbmac-custom-size]').forEach(el=>el.addEventListener('change',syncSummary));
  syncPersCount();
  syncSummary();
})();
</script>

{% if section.settings.show_reviews %}
<script>
(() => {
  const section=document.getElementById('pbmac-reviews-{{ section.id }}');
  if(!section||section.dataset.ready==='true')return;
  section.dataset.ready='true';

  const pageSize=10;
  const cards=[...section.querySelectorAll('[data-pbmac-review]')];
  const filters=[...section.querySelectorAll('.pbmac-review-filters button')];
  const pagination=section.querySelector('.pbmac-pagination');
  const modal=section.querySelector('.pbmac-review-modal');
  const content=section.querySelector('.pbmac-review-modal__content');
  const prev=section.querySelector('[data-pbmac-review-prev]');
  const next=section.querySelector('[data-pbmac-review-next]');
  const images=section.querySelector('[data-pbmac-review-images]');
  const imagesPrev=section.querySelector('[data-pbmac-images-prev]');
  const imagesNext=section.querySelector('[data-pbmac-images-next]');
  const imageCards=[...section.querySelectorAll('[data-pbmac-image-review-index]')];

  let rating='all',page=1,modalIndex=-1;

  const filtered=()=>rating==='all'?cards:cards.filter(c=>c.dataset.rating===rating);

  const renderPages=()=>{
    const list=filtered(),pages=Math.max(1,Math.ceil(list.length/pageSize));
    if(page>pages)page=pages;
    pagination.innerHTML='';
    if(list.length<=pageSize){pagination.hidden=true;return}
    pagination.hidden=false;

    const mk=(label,p,active=false,disabled=false)=>{
      const b=document.createElement('button');
      b.type='button';
      b.textContent=label;
      b.dataset.page=p;
      b.disabled=disabled;
      if(active)b.classList.add('is-active');
      return b;
    };

    pagination.appendChild(mk('‹',page-1,false,page===1));
    for(let i=1;i<=pages;i++)pagination.appendChild(mk(String(i),i,i===page));
    pagination.appendChild(mk('›',page+1,false,page===pages));
  };

  const render=(scroll=false)=>{
    const list=filtered(),start=(page-1)*pageSize,current=list.slice(start,start+pageSize);
    cards.forEach(c=>c.hidden=!current.includes(c));
    renderPages();

    if(scroll){
      section.querySelector('.pbmac-topreviews-head')?.scrollIntoView({behavior:'smooth',block:'start'});
    }
  };

  filters.forEach(f=>f.addEventListener('click',()=>{
    if(f.disabled)return;
    filters.forEach(x=>x.classList.remove('is-active'));
    f.classList.add('is-active');
    rating=f.dataset.rating||'all';
    page=1;
    render();
  }));

  pagination?.addEventListener('click',e=>{
    const b=e.target.closest('button[data-page]');
    if(!b||b.disabled)return;
    page=Number(b.dataset.page||1);
    render(true);
  });

  const close=()=>{
    modal.hidden=true;
    document.body.classList.remove('pbmac-modal-open');
    modalIndex=-1;
  };

  const openAt=i=>{
    const list=filtered();
    if(!list.length)return;
    modalIndex=(i+list.length)%list.length;
    const card=list[modalIndex];
    const src=card.querySelector('[data-pbmac-review-modal-source]');
    if(!src)return;
    content.innerHTML='';
    content.appendChild(src.content.cloneNode(true));
    prev.hidden=list.length<=1;
    next.hidden=list.length<=1;
    modal.hidden=false;
    document.body.classList.add('pbmac-modal-open');
  };

  const openCard=card=>{
    const list=filtered();
    const idx=list.indexOf(card);
    if(idx>=0)openAt(idx);
  };

  cards.forEach(c=>{
    c.addEventListener('click',()=>openCard(c));
    c.addEventListener('keydown',e=>{
      if(e.key==='Enter'||e.key===' '){
        e.preventDefault();
        openCard(c);
      }
    });
  });

  imageCards.forEach(imageCard=>{
    imageCard.addEventListener('click',()=>{
      const reviewIndex=Number(imageCard.dataset.pbmacImageReviewIndex||0);
      const card=cards.find(c=>Number(c.dataset.reviewIndex)===reviewIndex);
      if(!card)return;

      if(rating!=='all'){
        rating='all';
        filters.forEach(btn=>btn.classList.toggle('is-active',btn.dataset.rating==='all'));
      }
      openAt(cards.indexOf(card));
    });
  });

  imagesPrev?.addEventListener('click',()=>images?.scrollBy({left:-520,behavior:'smooth'}));
  imagesNext?.addEventListener('click',()=>images?.scrollBy({left:520,behavior:'smooth'}));

  section.querySelectorAll('[data-pbmac-review-close]').forEach(x=>x.addEventListener('click',close));
  prev?.addEventListener('click',()=>openAt(modalIndex-1));
  next?.addEventListener('click',()=>openAt(modalIndex+1));

  document.addEventListener('keydown',e=>{
    if(!modal||modal.hidden)return;
    if(e.key==='Escape')close();
    if(e.key==='ArrowLeft')openAt(modalIndex-1);
    if(e.key==='ArrowRight')openAt(modalIndex+1);
  });

  render();
})();
</script>
{% endif %}

{% schema %}
{
  "name": "Plastbat Shirt PDP20",
  "tag": "section",
  "class": "section",
  "enabled_on": {
    "templates": [
      "product"
    ]
  },
  "settings": [
    {
      "type": "header",
      "content": "Style"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "checkbox",
      "id": "sticky_gallery",
      "label": "Sticky gallery",
      "default": true
    },
    {
      "type": "range",
      "id": "sticky_top",
      "label": "Sticky top offset",
      "min": 50,
      "max": 160,
      "step": 5,
      "unit": "px",
      "default": 90
    },
    {
      "type": "header",
      "content": "Option visibility"
    },
    {
      "type": "checkbox",
      "id": "show_style_option",
      "label": "Show Style",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_color_option",
      "label": "Show Color",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_size_option",
      "label": "Show Size",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_save_badge",
      "label": "Show Save % beside price",
      "default": false
    },
    {
      "type": "header",
      "content": "Personalization"
    },
    {
      "type": "checkbox",
      "id": "show_personalization",
      "label": "Show personalization",
      "default": true
    },
    {
      "type": "text",
      "id": "personalization_heading",
      "label": "Personalization heading",
      "default": "Personalized"
    },
    {
      "type": "text",
      "id": "personalization_field_label",
      "label": "Personalization field label",
      "default": "Enter Your Text"
    },
    {
      "type": "text",
      "id": "personalization_helper",
      "label": "Personalization helper",
      "default": "Press Space On The Keyboard If You Don't Want To Include Text"
    },
    {
      "type": "text",
      "id": "personalization_placeholder",
      "label": "Personalization placeholder",
      "default": "Enter your custom text"
    },
    {
      "type": "checkbox",
      "id": "personalization_required",
      "label": "Require personalization",
      "default": false
    },
    {
      "type": "range",
      "id": "personalization_max",
      "label": "Maximum characters",
      "min": 5,
      "max": 50,
      "step": 1,
      "default": 20
    },
    {
      "type": "checkbox",
      "id": "show_preview_button",
      "label": "Show Preview button",
      "default": true
    },
    {
      "type": "text",
      "id": "preview_button_text",
      "label": "Preview button text",
      "default": "Preview"
    },
    {
      "type": "header",
      "content": "Purchase"
    },
    {
      "type": "text",
      "id": "quantity_label",
      "label": "Quantity label",
      "default": "Quantity"
    },
    {
      "type": "text",
      "id": "customization_summary_heading",
      "label": "Customization summary heading",
      "default": "Your customization"
    },
    {
      "type": "text",
      "id": "customization_summary_empty",
      "label": "Customization summary empty text",
      "default": "Add your text above"
    },
    {
      "type": "text",
      "id": "default_color_label",
      "label": "Default color label",
      "default": "Selected color"
    },
    {
      "type": "text",
      "id": "personalized_add_to_cart_text",
      "label": "Personalized add to cart text",
      "default": "PERSONALIZE & ADD TO CART"
    },
    {
      "type": "text",
      "id": "add_to_cart_text",
      "label": "Add to Cart text",
      "default": "Add to Cart"
    },
    {
      "type": "text",
      "id": "sold_out_text",
      "label": "Sold out text",
      "default": "Sold out"
    },
    {
      "type": "text",
      "id": "trust_1",
      "label": "Trust line 1",
      "default": "Full refund for faulty production. No question asked"
    },
    {
      "type": "text",
      "id": "trust_2",
      "label": "Trust line 2",
      "default": "Made to order with secure checkout"
    },
    {
      "type": "header",
      "content": "Size chart"
    },
    {
      "type": "checkbox",
      "id": "show_size_chart",
      "label": "Show Size Chart",
      "default": true
    },
    {
      "type": "header",
      "content": "Accordions"
    },
    {
      "type": "text",
      "id": "description_heading",
      "label": "Description heading",
      "default": "Description"
    },
    {
      "type": "richtext",
      "id": "description_body",
      "label": "Description",
      "default": "<p>Add your product description here, or clear this field to use the Shopify product description.</p>"
    },
    {
      "type": "checkbox",
      "id": "open_description",
      "label": "Open description by default",
      "default": false
    },
    {
      "type": "text",
      "id": "shipping_heading",
      "label": "Shipping heading",
      "default": "Shipping & Returns"
    },
    {
      "type": "richtext",
      "id": "shipping_text",
      "label": "Shipping text",
      "default": "<p>Add your processing, delivery, return and replacement policy here.</p>"
    },
    {
      "type": "text",
      "id": "personalization_info_heading",
      "label": "Personalization info heading",
      "default": "Personalization"
    },
    {
      "type": "richtext",
      "id": "personalization_info",
      "label": "Personalization info",
      "default": "<p>Please review all custom text and selected options before checkout. Preview colors may vary slightly from the finished item.</p>"
    },
    {
      "type": "header",
      "content": "Reviews"
    },
    {
      "type": "checkbox",
      "id": "show_reviews",
      "label": "Show reviews",
      "default": true
    },
    {
      "type": "text",
      "id": "reviews_heading",
      "label": "Reviews heading",
      "default": "Customer Reviews"
    },
    {
      "type": "header",
      "content": "Newsletter near footer"
    },
    {
      "type": "checkbox",
      "id": "show_newsletter",
      "label": "Show newsletter signup",
      "default": true
    },
    {
      "type": "text",
      "id": "newsletter_heading",
      "label": "Newsletter heading",
      "default": "Unlock Your 10% OFF!"
    },
    {
      "type": "text",
      "id": "newsletter_subtext",
      "label": "Newsletter subtext",
      "default": "Join us for gift inspiration, extra savings, and alerts on our latest drops."
    },
    {
      "type": "text",
      "id": "newsletter_placeholder",
      "label": "Email placeholder",
      "default": "Enter your email"
    },
    {
      "type": "text",
      "id": "newsletter_button",
      "label": "Subscribe button",
      "default": "Subscribe"
    },
    {
      "type": "text",
      "id": "newsletter_success",
      "label": "Success message",
      "default": "Thanks for subscribing!"
    },
    {
      "type": "header",
      "content": "Spacing"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Top padding",
      "min": 0,
      "max": 80,
      "step": 4,
      "unit": "px",
      "default": 24
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Bottom padding",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "default": 28
    },
    {
      "type": "header",
      "content": "More items section"
    },
    {
      "type": "checkbox",
      "id": "show_more_items",
      "label": "Show more items to consider",
      "default": true
    },
    {
      "type": "text",
      "id": "more_items_heading",
      "label": "More items heading",
      "default": "More Items to Consider"
    },
    {
      "type": "collection",
      "id": "more_items_collection",
      "label": "More items collection"
    },
    {
      "type": "range",
      "id": "more_items_limit",
      "label": "Number of items",
      "min": 2,
      "max": 8,
      "step": 1,
      "default": 4
    }
  ],
  "blocks": [
    {
      "type": "manual_review",
      "name": "Manual review",
      "settings": [
        {
          "type": "text",
          "id": "author",
          "label": "Customer name",
          "default": "Customer"
        },
        {
          "type": "range",
          "id": "rating",
          "label": "Rating",
          "min": 1,
          "max": 5,
          "step": 1,
          "default": 5
        },
        {
          "type": "text",
          "id": "date",
          "label": "Date"
        },
        {
          "type": "checkbox",
          "id": "verified",
          "label": "Verified Purchase",
          "default": false
        },
        {
          "type": "text",
          "id": "title",
          "label": "Review title"
        },
        {
          "type": "richtext",
          "id": "body",
          "label": "Review text"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Customer photo"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Plastbat Shirt PDP20"
    }
  ]
}
{% endschema %}
